import type { SectionHeading } from "@/lib/config/content/types";

export type TechStackContent = SectionHeading;

export const techStack: TechStackContent = {
  eyebrow: "Powered by",
  title: "Standing on open, honest tools",
  description:
    "SigNoz is the operating system for the whole investigation. Everything the agent sees, does and records lives there.",
};
