# Exporting invoice history as CSV

Workspace Settings > Billing > Invoice History has an "Export CSV" button in the top-right that downloads every invoice for the current calendar year, including invoice date, amount, status, and payment method last four digits. For prior years, use the year selector dropdown above the table before exporting - each export is scoped to one calendar year at a time.
