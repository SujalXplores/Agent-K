# Creating your first board

From the workspace home screen, click "New Project" and choose the Board template, which starts you with three default columns (To Do, In Progress, Done). Add tasks with the "+" button in any column, and drag tasks between columns to change their status - each drag automatically updates the task's status field, which is what the GitHub commit-message auto-transition and Jira sync both key off of.
