# Bulk-editing multiple tasks at once

Select multiple tasks using the checkbox that appears on hover (or Shift-click to select a range), then use the bulk action bar that appears at the bottom of the screen to change status, assignee, due date, or labels across all selected tasks in one action. Bulk edits are logged individually in each task's Activity tab, not just as one combined entry, so per-task history stays complete.
