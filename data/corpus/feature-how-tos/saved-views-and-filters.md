# Creating saved views and filters

Apply any combination of filters (assignee, label, custom field, due date range) using the Filter button above a board, then click "Save as view" to preserve that exact filter combination as a named tab you can return to later. Saved views are per-member by default, but can be shared to the whole project team via the "Share view" toggle.
