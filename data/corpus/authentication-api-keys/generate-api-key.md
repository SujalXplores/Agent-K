# How do I generate an API key?

Go to Workspace Settings > Developer > API Keys and click "Generate new key". Give it a descriptive label (e.g. "CI pipeline" or "Zapier integration") since you can't recover the key value after closing the dialog - only the label and creation date remain visible afterward, so store the key in a secrets manager immediately.
