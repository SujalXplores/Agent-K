# Configuring session timeout

Workspace owners can set an inactivity timeout under Workspace Settings > Security > Session Timeout, ranging from 1 hour to 30 days (default is 14 days). Shortening the timeout does not immediately log out already-active sessions - it only applies to sessions created after the change, so existing logged-in members keep their prior timeout until they log out or their token naturally expires.
