# Transferring project ownership

Every project has exactly one Owner (defaults to whoever created it), separate from the workspace-level Owner role. Transfer it from Project Settings > "Transfer ownership", which requires the recipient to already be a project Admin or Editor before they can be made Owner - you can't transfer directly to someone with no existing access to the project.
