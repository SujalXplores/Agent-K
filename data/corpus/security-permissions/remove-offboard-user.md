# Removing or offboarding a member

Remove a member from Workspace Settings > Members > find the member > "Remove from workspace". This immediately revokes all workspace and project access and any personal access tokens they had generated, but preserves their name on past task comments and activity history (shown as "[Former member]" rather than being deleted) so historical records stay intact.
