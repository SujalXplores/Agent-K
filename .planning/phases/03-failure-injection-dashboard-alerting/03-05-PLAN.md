---
phase: 03-failure-injection-dashboard-alerting
plan: 05
type: execute
wave: 3
depends_on: ["03-01", "03-02", "03-03", "03-04"]
files_modified:
  - SIGNOZ-RUNBOOK.md
  - .planning/phases/03-failure-injection-dashboard-alerting/dashboard.json
  - .planning/phases/03-failure-injection-dashboard-alerting/alerts.json
autonomous: false
requirements: [DASH-01, DASH-02, DASH-05]
must_haves:
  truths:
    - "SIGNOZ-RUNBOOK.md documents exact, reproducible steps to hand-build the Service Health + Incident Context dashboard sections, the SLO/burn-rate/cost alert rules, the webhook channel, and the JSON export"
    - "After HV-2, dashboard.json and alerts.json exported from the live SigNoz UI are committed to the repo (DASH-01 requires committed JSON)"
    - "A live SLO/burn-rate/cost alert breach fires a webhook that POST /alerts/webhook receives end-to-end (Success Criterion 4)"
    - "The deployment.marker asymmetry (scenarios 1-2 present, 3-4 absent) is visible in the Incident Context section (Success Criterion 2)"
  artifacts:
    - path: "SIGNOZ-RUNBOOK.md"
      provides: "Phase-3 addendum: dashboard/alert/webhook build steps + export steps + end-to-end verification checklist"
      contains: "Phase 3"
    - path: ".planning/phases/03-failure-injection-dashboard-alerting/dashboard.json"
      provides: "Exported SigNoz dashboard JSON (Service Health + Incident Context)"
      min_lines: 1
    - path: ".planning/phases/03-failure-injection-dashboard-alerting/alerts.json"
      provides: "Exported SigNoz alert-rule config JSON"
      min_lines: 1
  key_links:
    - from: "SigNoz alert rule (SLO/burn-rate/cost)"
      to: "POST /alerts/webhook (app/alerts_webhook.py)"
      via: "SigNoz webhook channel targeting the reachable app endpoint over the compose network"
      pattern: "alerts/webhook"
    - from: "Incident Context dashboard panel"
      to: "deployment.marker span (emitted by app/flags.py)"
      via: "Trace/Logs Explorer panel filtered on span name = deployment.marker"
      pattern: "deployment.marker"
---

<objective>
Capture the SigNoz-UI half of this phase (DASH-01, DASH-02, DASH-05) as reproducible human-action
runbook steps, then execute them against the live SigNoz stack (HV-2) to build the dashboard + alert
rules, confirm the end-to-end webhook fire, and commit the exported JSON. Per the locked manual-UI-vs-code
split, all dashboards/alerts are hand-built in the SigNoz UI (dashboard-as-code is out of scope);
Claude automates writing the runbook, the human executes against the live stack. This plan is the
only non-autonomous plan in the phase — it is gated on HV-2 (live Foundry SigNoz), which is still
open from Phase 2.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-failure-injection-dashboard-alerting/03-CONTEXT.md
@.planning/phases/03-failure-injection-dashboard-alerting/03-RESEARCH.md
@.planning/REQUIREMENTS.md
@SIGNOZ-RUNBOOK.md
@app/observability.py
@app/alerts_webhook.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write the Phase-3 SigNoz build + export runbook addendum</name>
  <files>SIGNOZ-RUNBOOK.md</files>
  <read_first>SIGNOZ-RUNBOOK.md (existing section structure, sec 1.5 first-run setup, Quick reference table), 03-RESEARCH.md Key Findings 2/3/4 (webhook payload, dashboard export/import mechanics, SLO/burn-rate has no first-party object — hand-computed threshold query), app/observability.py (exact attribute names the panels/alerts must query: RAG_RETRIEVAL_LATENCY_INJECTED, RAG_PROMPT_REGRESSION_ACTIVE, AGENTK_LLM_RETRY_COUNT, AGENTK_DB_POOL_EXHAUSTED, DEPLOYMENT_MARKER_SCENARIO, gen_ai.*/agentk.llm.* cost/token attrs), app/alerts_webhook.py (endpoint path + expected payload)</read_first>
  <action>Append a "Phase 3 — Failure Injection Dashboard & Alerts" section to SIGNOZ-RUNBOOK.md with numbered, reproducible steps for: (1) DASH-01 Service Health panels — request rate, error rate, latency, retrieval latency (query rag.retrieval span duration), LLM latency (chat span duration), SLO status + burn rate (hand-computed threshold query per Key Finding 4, no native SLO object), current deployment version (query DEPLOYMENT_MARKER_VERSION); (2) DASH-02 Incident Context panels — active alerts, a Trace/Logs Explorer panel filtered on span name = "deployment.marker" (making the FLAG-06 scenarios-1-2-present / 3-4-absent asymmetry visible), incident start time, affected service, related trace IDs + deep links; (3) DASH-05 alert rules — a cost/call-rate alert querying AGENTK_LLM_RETRY_COUNT (retry-storm), a latency/burn-rate alert (retrieval-latency), and a webhook channel (SigNoz Settings → Alert Channels → Webhook) whose URL targets the app's /alerts/webhook reachable over the compose network; (4) the JSON export step (Dashboard menu → copy/download JSON, or GET /api/v2/dashboards/{id}) writing to .planning/phases/03-.../dashboard.json and the alert config to alerts.json; (5) an end-to-end verification checklist tying each step to Success Criteria 2/3/4 and a final "reset all flags to false via POST /admin/flags" cleanup step (T-03-04). Reference the concrete attribute-name constants by value so a human building panels copies the exact query keys. Do not invent a SigNoz deployment-marker API — panels read the custom deployment.marker span (Key Finding 1).</action>
  <verify>
    <automated>grep -c "deployment.marker" SIGNOZ-RUNBOOK.md | grep -qv '^0$' && grep -q "alerts/webhook" SIGNOZ-RUNBOOK.md && grep -q "AGENTK_LLM_RETRY_COUNT\|agentk.llm.retry_count" SIGNOZ-RUNBOOK.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - SIGNOZ-RUNBOOK.md contains a Phase-3 section covering DASH-01 Service Health, DASH-02 Incident Context, and DASH-05 alert rules + webhook channel + JSON export + reset-flags cleanup.
    - The runbook references the exact observability attribute constant values (retry_count, deployment.marker span name, retrieval latency attr) and the /alerts/webhook target.
    - The runbook does NOT instruct calling a nonexistent SigNoz deployment-marker API; Incident Context reads the deployment.marker span.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Code side complete (flags, four injectors, deployment marker, webhook receiver — plans 03-01..03-04, all offline-tested) and the Phase-3 SigNoz build/export runbook (Task 1). This checkpoint is the HV-2 live-stack execution: build the dashboard + alerts in the SigNoz UI, confirm the end-to-end webhook fire and the marker asymmetry, and commit the exported JSON.</what-built>
  <how-to-verify>
    1. Bring up the live SigNoz stack per SIGNOZ-RUNBOOK.md sec 1 + sec 1.5 first-run setup (POST /api/v1/register), then start the RAG app so it exports OTLP to the collector.
    2. Generate baseline traffic: POST /ask a few times; confirm rag.retrieval / rag.prompt_construction / chat spans render (Phase-2 HV-2).
    3. Follow the Phase-3 runbook to hand-build the Service Health + Incident Context dashboard sections (DASH-01/02); confirm request rate, error rate, latency, retrieval latency, LLM latency, SLO/burn-rate, and current deployment version panels populate.
    4. Toggle each scenario via POST /admin/flags (with X-Admin-Token if ADMIN_TOKEN set): confirm prompt-regression + retry-storm each produce a deployment.marker span in the Incident Context panel, and retrieval-latency + DB-pool-exhaustion produce NONE (Success Criterion 2 / FLAG-06). Confirm each fault's documented symptom is visible (broken-prompt regression_active, retry_count spike, slow rag.retrieval, pool-exhaustion errors correlated to failed traces) — Success Criterion 1/3.
    5. Build the SLO/burn-rate/cost alert rules + webhook channel targeting /alerts/webhook; toggle retry-storm to breach the cost/call-rate threshold; confirm SigNoz fires the webhook and the app's /alerts/webhook logs+persists a real SigNoz payload end-to-end (Success Criterion 4 / DASH-05).
    6. Export dashboard JSON → .planning/phases/03-failure-injection-dashboard-alerting/dashboard.json and alert config → alerts.json; commit both (DASH-01 requires committed JSON).
    7. Reset all flags to false via POST /admin/flags (cleanup, T-03-04).
    8. If the live Alertmanager payload shape differs from app/alerts_webhook.py's Pydantic model (RESEARCH Assumption A3), record the delta as a gap for a follow-up plan rather than loosening the schema silently.
  </how-to-verify>
  <resume-signal>Type "approved" once dashboard.json + alerts.json are committed and the end-to-end webhook fire + marker asymmetry are confirmed; or describe which criterion failed (and any payload-schema delta) so a gap-closure plan can be scoped.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SigNoz UI / alertmanager → app endpoints | Operator-built alert rules fire webhooks into the app; dashboard queries read exported telemetry |
| network → SigNoz UI ports | Live stack exposes UI/collector ports on the host |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-17 | Spoofing | SigNoz webhook channel → /alerts/webhook | mitigate | Payload validated by the Plan-04 Pydantic schema; runbook notes optional SigNoz webhook basic-auth if exposed beyond localhost |
| T-03-18 | Denial of Service (self-inflicted) | Flags left enabled after live demo | mitigate | Runbook's final step resets all flags to false via POST /admin/flags; GET /admin/flags audits state |
| T-03-19 | Information Disclosure | Exported dashboard/alert JSON committed to repo | mitigate | Export panel/alert config only (queries + thresholds); confirm no API keys, tokens, or raw payload content are embedded in dashboard.json/alerts.json before committing |
| T-03-20 | Tampering | Live SigNoz port exposure | accept | Covered by SIGNOZ-RUNBOOK.md sec 3 port-hardening caution (T-01-02); no new exposure introduced by this phase |
</threat_model>

<verification>
- Task 1: `grep` confirms the Phase-3 runbook section references deployment.marker, /alerts/webhook, and the retry_count attribute.
- Checkpoint (HV-2): dashboard.json + alerts.json committed; end-to-end webhook fire confirmed; deployment.marker asymmetry confirmed in the Incident Context section; all four fault symptoms observed live; flags reset.
</verification>

<success_criteria>
DASH-01: one hand-built SigNoz dashboard with a Service Health section, exported as committed JSON.
DASH-02: Incident Context section showing active alerts, deployment markers, incident start, affected service, related trace IDs + links.
DASH-05: SLO/burn-rate/cost alert rules wired to fire a webhook to /alerts/webhook, confirmed firing end-to-end.
(Success Criteria 2, 3, 4 verified live at HV-2.)
</success_criteria>

<output>
Create `.planning/phases/03-failure-injection-dashboard-alerting/03-05-SUMMARY.md` when done.
Record any live-payload-schema delta (A3) or unmet SigNoz criterion as a carried gap for a follow-up plan.
</output>
