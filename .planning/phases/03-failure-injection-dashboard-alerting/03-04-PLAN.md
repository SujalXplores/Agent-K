---
phase: 03-failure-injection-dashboard-alerting
plan: 04
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/alerts_webhook.py
  - app/main.py
  - tests/test_webhook.py
autonomous: true
requirements: [DASH-05]
must_haves:
  truths:
    - "POST /alerts/webhook accepts a well-formed SigNoz Alertmanager-shaped payload, logs each alert, persists it, and returns 200"
    - "A malformed payload (missing required fields / wrong types) is rejected with 422 before any processing"
    - "Persisted alert records expose the fields Agent K's Phase-5 loop needs (alert name, status, severity/labels, startsAt, affected service, trace/label context)"
    - "The route is registered before FastAPIInstrumentor.instrument_app so it is wrapped in spans"
  artifacts:
    - path: "app/alerts_webhook.py"
      provides: "APIRouter with POST /alerts/webhook, narrow Alertmanager Pydantic models, validate->log->persist, get_alerts() accessor"
      contains: "class AlertmanagerWebhookPayload"
    - path: "app/main.py"
      provides: "app.include_router(alerts_webhook.router) registered before instrument_app"
      contains: "alerts_webhook"
    - path: "tests/test_webhook.py"
      provides: "valid-payload persist test + malformed-payload 422 test + secret-not-logged test"
      contains: "def test_"
  key_links:
    - from: "app/main.py"
      to: "app/alerts_webhook.py router"
      via: "app.include_router before FastAPIInstrumentor.instrument_app"
      pattern: "include_router\\(alerts_webhook"
    - from: "POST /alerts/webhook handler"
      to: "in-process alert store"
      via: "validate (Pydantic) -> log -> _persist_alert"
      pattern: "_persist_alert|get_alerts"
---

<objective>
Build the reusable alert-webhook entrypoint (DASH-05 code half) as its own module: a real
`POST /alerts/webhook` that validates a narrow SigNoz Alertmanager-shaped payload, logs it (never
logging secrets), and persists it. This is deliberately not a throwaway — it is designed to be the
actual trigger Agent K's Phase-5 investigation loop consumes (INV-01), so proving the entry path
end-to-end now de-risks the reduced-capacity Phase-5 window. The SigNoz-UI half of DASH-05 (building
the alert rule + webhook channel and confirming an end-to-end fire) is human-action in Plan 05.

Purpose: A validated, persisted, span-wrapped inbound alert path.
Output: new `app/alerts_webhook.py`, extended `app/main.py`, new `tests/test_webhook.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-failure-injection-dashboard-alerting/03-CONTEXT.md
@.planning/phases/03-failure-injection-dashboard-alerting/03-RESEARCH.md
@app/main.py
@app/schemas.py

<interfaces>
SigNoz webhook body shape (RESEARCH Key Finding 2 / "Webhook receiver schema"), Alertmanager-shaped:
top-level: receiver:str, status:str, alerts:list[AlertItem], groupLabels:dict[str,str]={},
commonLabels:dict[str,str]={}, commonAnnotations:dict[str,str]={}, externalURL:str|None,
version:str|None, groupKey:str|None, truncatedAlerts:int|None.
AlertItem: status:str, labels:dict[str,str], annotations:dict[str,str], startsAt:str,
endsAt:str|None, generatorURL:str|None, fingerprint:str|None.
app/main.py registers routes in step 2, THEN setup_telemetry()/FastAPIInstrumentor.instrument_app in
steps 3-4 — the router include MUST go in step 2 (03-01 already added /admin/flags there).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: alerts_webhook module — Pydantic models, route, persist + accessor</name>
  <files>app/alerts_webhook.py, tests/test_webhook.py</files>
  <read_first>03-RESEARCH.md "Webhook receiver schema" code example + Key Finding 2, app/schemas.py (Pydantic style), app/observability.py content-safety note (never log/attribute secrets), tests/conftest.py (TestClient pattern via `client` / plain fastapi.testclient.TestClient)</read_first>
  <behavior>
    - POST /alerts/webhook with a valid Alertmanager-shaped body returns 200 with {"received": <len(alerts)>} and every alert is persisted; get_alerts() then returns those records.
    - POST with a body missing a required field (e.g. no `receiver`, or `alerts` not a list, or an AlertItem missing `startsAt`) returns 422 and nothing is persisted.
    - Each persisted record carries the Agent-K-relevant fields: alert name (from labels/groupLabels "alertname"), status, labels, annotations, startsAt, fingerprint.
    - Logging emits only alert name + status (non-sensitive lifecycle info) — the raw annotations/labels values are persisted for Phase 5 but the log line does not dump a full secret-bearing payload.
  </behavior>
  <action>Create app/alerts_webhook.py with an APIRouter and the narrow Pydantic models AlertItem and AlertmanagerWebhookPayload exactly per the interfaces block (strict field set — do NOT build a generic/flexible parser; over-generalizing is the RESEARCH "Don't Hand-Roll" anti-pattern). Add a module-level in-memory store (list) with `_persist_alert(receiver, alert)` appending a record dict of the Agent-K-relevant fields and `get_alerts()` returning the list (planner's-discretion persistence: in-memory list is sufficient for DASH-05 validate/log/persist). The `@router.post("/alerts/webhook")` handler iterates payload.alerts, logs alert name + status via logging.getLogger(__name__).info (no full-payload dump), persists each, and returns {"received": len(payload.alerts)}. Write tests/test_webhook.py covering all four behavior bullets with a fastapi.testclient.TestClient built directly on a minimal FastAPI app that includes the router (or on app.main.app), including a malformed-payload 422 assertion and an assertion that a secret-looking annotation value never appears in captured log output (use caplog).</action>
  <verify>
    <automated>pytest tests/test_webhook.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/alerts_webhook.py contains class AlertmanagerWebhookPayload` and `def get_alerts(` and `@router.post("/alerts/webhook")`.
    - Test proves: valid payload → 200 + persisted + get_alerts non-empty; malformed → 422 + nothing persisted; secret annotation value not present in log records.
    - `pytest tests/test_webhook.py -q` exits 0.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Register the webhook router in main.py before instrumentation</name>
  <files>app/main.py, tests/test_webhook.py</files>
  <read_first>app/main.py (numbered registration comments; /admin/flags already added in step 2 by 03-01), app/alerts_webhook.py (router)</read_first>
  <action>In app/main.py, import app.alerts_webhook and add `app.include_router(alerts_webhook.router)` in the step-2 route-registration region — textually BEFORE setup_telemetry() and FastAPIInstrumentor.instrument_app(app). Do not alter the existing ordering of setup_telemetry / instrument_app / setup_db_instrumentation / LoggingInstrumentor. Extend tests/test_webhook.py with a test that drives the route through app.main.app's TestClient to confirm it is reachable and span-wrapped (route resolves, returns 200 on a valid body) — proving registration landed before instrument_app.</action>
  <verify>
    <automated>pytest tests/test_webhook.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/main.py` contains `include_router(alerts_webhook.router)` and that line appears BEFORE `FastAPIInstrumentor.instrument_app(app)` (grep line-order check).
    - A TestClient against app.main.app returns 200 for a valid POST /alerts/webhook.
    - `pytest tests/test_webhook.py -q` exits 0; full offline suite `pytest tests/ -m "not integration" -q` green.
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| SigNoz alertmanager / network → POST /alerts/webhook | Unauthenticated inbound endpoint receiving arbitrary POSTed JSON |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-13 | Spoofing / Tampering | POST /alerts/webhook payload | mitigate | Strict narrow Pydantic schema rejects malformed/unexpected bodies with 422 before any processing |
| T-03-14 | Denial of Service | Unbounded inbound alerts | accept | 4 seeded incidents fire one at a time (no alert-correlation scope); in-memory list is bounded by demo volume; note in runbook that SigNoz webhook basic-auth may be enabled if exposed beyond localhost |
| T-03-15 | Information Disclosure | Logging the payload | mitigate | Log only alert name + status; never dump full labels/annotations (may carry sensitive context); asserted by a caplog test |
| T-03-16 | Tampering | Route added after instrument_app | mitigate | include_router placed in main.py step 2, before instrument_app (line-order test) |
</threat_model>

<verification>
- `pytest tests/test_webhook.py -q` green (valid persist, malformed 422, no-secret-logging, reachable via app.main).
- Grep confirms include_router precedes FastAPIInstrumentor.instrument_app in app/main.py.
- (Deferred to HV-2, Plan 05) A real SigNoz alert rule fires an end-to-end webhook into this endpoint.
</verification>

<success_criteria>
DASH-05 (code half): a validated, logged, persisted, span-wrapped POST /alerts/webhook that Phase 5's investigation loop can consume as its trigger.
</success_criteria>

<output>
Create `.planning/phases/03-failure-injection-dashboard-alerting/03-04-SUMMARY.md` when done.
</output>
