---
phase: 03-failure-injection-dashboard-alerting
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/rag.py
  - tests/test_injection_rag.py
autonomous: true
requirements: [FLAG-02, FLAG-04]
must_haves:
  truths:
    - "With prompt_regression ON, build_prompt returns the broken system prompt and the rag.prompt_construction span carries regression_active=true"
    - "With prompt_regression OFF, build_prompt returns the unchanged D-05 SYSTEM_PROMPT and regression_active=false"
    - "With retrieval_latency ON, retrieve() sleeps the injected delay and the rag.retrieval span carries latency_injected=true; OFF adds no delay and latency_injected=false"
  artifacts:
    - path: "app/rag.py"
      provides: "flag-gated prompt swap in build_prompt and flag-gated delay in retrieve()"
      contains: "flags.is_enabled"
    - path: "tests/test_injection_rag.py"
      provides: "in_memory_exporter tests proving both injected symptoms change emitted spans"
      contains: "def test_prompt_regression"
  key_links:
    - from: "app/rag.py build_prompt"
      to: "app/flags.py is_enabled + app/observability.py RAG_PROMPT_REGRESSION_ACTIVE"
      via: "fresh is_enabled('prompt_regression') read + span.set_attribute"
      pattern: "RAG_PROMPT_REGRESSION_ACTIVE"
    - from: "app/rag.py retrieve"
      to: "app/flags.py is_enabled + app/observability.py RAG_RETRIEVAL_LATENCY_INJECTED"
      via: "fresh is_enabled('retrieval_latency') read + asyncio.sleep + span.set_attribute"
      pattern: "RAG_RETRIEVAL_LATENCY_INJECTED"
---

<objective>
Inject the two retrieval-tier fault scenarios that live in app/rag.py: prompt-regression (FLAG-02)
and retrieval-latency (FLAG-04). Both read their flag fresh per call and stamp a new span attribute
so the symptom is queryable in SigNoz, not merely a behavioral change SigNoz cannot see (RESEARCH
Pitfall 2: a broken prompt still returns HTTP 200 — the regression_active attribute is the concrete
signal the Incident Context dashboard filters on).

Purpose: Make two of the four seeded incidents toggleable live and observable in the trace waterfall.
Output: extended `app/rag.py`, new `tests/test_injection_rag.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-failure-injection-dashboard-alerting/03-CONTEXT.md
@.planning/phases/03-failure-injection-dashboard-alerting/03-RESEARCH.md
@app/rag.py
@app/flags.py
@app/observability.py

<interfaces>
Consumed from 03-01 (already built): app.flags.is_enabled(name) -> bool;
app.observability.RAG_PROMPT_REGRESSION_ACTIVE, app.observability.RAG_RETRIEVAL_LATENCY_INJECTED.
Existing app/rag.py spans: retrieve() opens "rag.retrieval"; build_prompt() opens
"rag.prompt_construction". Both acquire the tracer fresh via trace.get_tracer(__name__) — keep that.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prompt-regression injector in build_prompt (FLAG-02)</name>
  <files>app/rag.py, tests/test_injection_rag.py</files>
  <read_first>app/rag.py (SYSTEM_PROMPT definition + build_prompt span body), app/flags.py (is_enabled), app/observability.py (RAG_PROMPT_REGRESSION_ACTIVE), tests/conftest.py (in_memory_exporter, stub_documents, reset_flags fixtures)</read_first>
  <behavior>
    - flags.is_enabled("prompt_regression") False (default): build_prompt returns (SYSTEM_PROMPT, user_prompt) and the rag.prompt_construction span has RAG_PROMPT_REGRESSION_ACTIVE == False.
    - flags.is_enabled("prompt_regression") True: build_prompt returns (BROKEN_SYSTEM_PROMPT, user_prompt) where BROKEN_SYSTEM_PROMPT != SYSTEM_PROMPT, and the span has RAG_PROMPT_REGRESSION_ACTIVE == True.
    - The user_prompt / context-assembly logic is byte-for-byte unchanged between the two branches (only the system prompt swaps).
  </behavior>
  <action>In app/rag.py add a module-level BROKEN_SYSTEM_PROMPT constant — a deliberately grounding-defeating variant (instructs the model to ignore the context docs, answer from general knowledge, and never say it does not know), kept obviously different from the D-05 SYSTEM_PROMPT per that file's existing header note. In build_prompt, read `regression_active = flags.is_enabled("prompt_regression")` fresh inside the rag.prompt_construction span, select BROKEN_SYSTEM_PROMPT when true else SYSTEM_PROMPT, set span attribute RAG_PROMPT_REGRESSION_ACTIVE=regression_active, and return the selected system prompt. Do not otherwise change the context-assembly or the returned user_prompt. Create tests/test_injection_rag.py::test_prompt_regression covering both behavior bullets via in_memory_exporter span-attribute assertions.</action>
  <verify>
    <automated>pytest tests/test_injection_rag.py::test_prompt_regression -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/rag.py contains BROKEN_SYSTEM_PROMPT` and `flags.is_enabled("prompt_regression")`.
    - Test asserts the returned system prompt differs between flag states and that RAG_PROMPT_REGRESSION_ACTIVE is set on the rag.prompt_construction span in both states.
    - `pytest tests/test_injection_rag.py::test_prompt_regression -q` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Retrieval-latency injector in retrieve (FLAG-04)</name>
  <files>app/rag.py, tests/test_injection_rag.py</files>
  <read_first>app/rag.py (retrieve() span body), app/flags.py (is_enabled), app/observability.py (RAG_RETRIEVAL_LATENCY_INJECTED), tests/conftest.py (fake_session, in_memory_exporter, reset_flags)</read_first>
  <behavior>
    - flags.is_enabled("retrieval_latency") False: retrieve() adds no artificial delay; the rag.retrieval span has RAG_RETRIEVAL_LATENCY_INJECTED == False; docs still returned from the (faked) session.
    - flags.is_enabled("retrieval_latency") True: retrieve() awaits an injected delay before the query and the rag.retrieval span has RAG_RETRIEVAL_LATENCY_INJECTED == True.
    - The delay is a module-level constant (small enough to keep the test fast, e.g. patchable) so the test does not sleep multiple real seconds.
  </behavior>
  <action>In app/rag.py add a module-level RETRIEVAL_LATENCY_INJECT_S constant and, inside retrieve()'s rag.retrieval span, read `flags.is_enabled("retrieval_latency")` fresh; when true, `await asyncio.sleep(RETRIEVAL_LATENCY_INJECT_S)` and set span attribute RAG_RETRIEVAL_LATENCY_INJECTED=True; always set the attribute (False when off). Import asyncio. Keep the existing embed_text + pgvector select + top_k logic unchanged and still layered under the free SQLAlchemy DB span. In tests/test_injection_rag.py::test_retrieval_latency, monkeypatch RETRIEVAL_LATENCY_INJECT_S to a tiny value (or assert on the attribute + that asyncio.sleep was awaited), using fake_session so no live DB is needed, and assert the attribute in both flag states.</action>
  <verify>
    <automated>pytest tests/test_injection_rag.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/rag.py contains RETRIEVAL_LATENCY_INJECT_S` and `flags.is_enabled("retrieval_latency")` and `asyncio.sleep`.
    - Test asserts RAG_RETRIEVAL_LATENCY_INJECTED True/False on the rag.retrieval span per flag state without sleeping multiple real seconds.
    - `pytest tests/test_injection_rag.py -q` exits 0 (both scenarios).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flag state → rag.py injection points | A stuck-on flag degrades answer quality / retrieval latency of the monitored service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06 | Tampering | build_prompt broken-prompt path | mitigate | Flag defaults OFF; broken prompt only selected when prompt_regression explicitly toggled (gated by 03-01's token'd admin endpoint) |
| T-03-07 | Denial of Service | retrieve() injected sleep | mitigate | Flag defaults OFF; delay only on explicit retrieval_latency toggle; bounded constant, not caller-controlled |
| T-03-08 | Information Disclosure | span attributes | mitigate | Only booleans/counts set as attributes (RAG_PROMPT_REGRESSION_ACTIVE, RAG_RETRIEVAL_LATENCY_INJECTED); no prompt/answer text — preserves app/observability.py content-safety discipline |
</threat_model>

<verification>
- `pytest tests/test_injection_rag.py -q` green (both scenarios, both flag states).
- Full offline suite `pytest tests/ -m "not integration" -q` still green (no regression to existing /ask spans).
- (Deferred to HV-2) Toggle each flag against live SigNoz and confirm a slow rag.retrieval span / regression_active attribute in the trace explorer.
</verification>

<success_criteria>
FLAG-02: prompt_regression toggle swaps the system prompt and stamps regression_active on the span.
FLAG-04: retrieval_latency toggle injects a visible delay and stamps latency_injected on rag.retrieval.
</success_criteria>

<output>
Create `.planning/phases/03-failure-injection-dashboard-alerting/03-02-SUMMARY.md` when done.
</output>
