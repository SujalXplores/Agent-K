---
phase: 03-failure-injection-dashboard-alerting
plan: 03
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - app/llm.py
  - app/db.py
  - tests/test_injection_llm_db.py
autonomous: true
requirements: [FLAG-03, FLAG-05]
must_haves:
  truths:
    - "With retry_storm ON, generate() makes several completion attempts per /ask, stamps agentk.llm.retry_count on the chat span, and still returns an answer (HTTP 200, error rate not raised)"
    - "With retry_storm OFF, generate() makes exactly one attempt and retry_count == 1"
    - "With db_pool_exhaustion ON, concurrent get_session checkouts beyond the limiter raise a pool-exhaustion-shaped error that is logged and flagged on the active span; OFF behaves normally"
  artifacts:
    - path: "app/llm.py"
      provides: "flag-gated lowered-timeout retry loop inside the chat span with retry_count attribute"
      contains: "flags.is_enabled"
    - path: "app/db.py"
      provides: "flag-gated application-level concurrency limiter in get_session (semaphore, no engine rebuild)"
      contains: "flags.is_enabled"
    - path: "tests/test_injection_llm_db.py"
      provides: "retry-storm attempt-count test (mocked provider) + concurrent pool-exhaustion test"
      contains: "def test_retry_storm"
  key_links:
    - from: "app/llm.py generate"
      to: "app/flags.py is_enabled + app/observability.py AGENTK_LLM_RETRY_COUNT"
      via: "fresh is_enabled('retry_storm') read + retry loop + span.set_attribute"
      pattern: "AGENTK_LLM_RETRY_COUNT"
    - from: "app/db.py get_session"
      to: "app/flags.py is_enabled + app/observability.py AGENTK_DB_POOL_EXHAUSTED"
      via: "fresh is_enabled('db_pool_exhaustion') read + asyncio.Semaphore + wait_for timeout"
      pattern: "AGENTK_DB_POOL_EXHAUSTED"
---

<objective>
Inject the two remaining fault scenarios: retry-storm (FLAG-03, in app/llm.py) and DB-pool-exhaustion
(FLAG-05, in app/db.py). Retry-storm's signal is a COST/CALL-RATE anomaly, not an availability one —
it must stay HTTP 200 while spiking attempt count (RESEARCH Pitfall 5). DB-pool-exhaustion uses an
application-level semaphore limiter rather than mutating the SQLAlchemy pool, because pool size is
fixed at engine construction and cannot resize live without a restart — the semaphore preserves
FLAG-01's no-restart guarantee uniformly (RESEARCH Pitfall 4, option b; Assumption A4 accepted).

Purpose: Complete the four toggleable seeded incidents; keep each symptom distinct and queryable.
Output: extended `app/llm.py`, extended `app/db.py`, new `tests/test_injection_llm_db.py`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-failure-injection-dashboard-alerting/03-CONTEXT.md
@.planning/phases/03-failure-injection-dashboard-alerting/03-RESEARCH.md
@app/llm.py
@app/db.py
@app/flags.py
@app/observability.py

<interfaces>
Consumed from 03-01: app.flags.is_enabled(name) -> bool;
app.observability.AGENTK_LLM_RETRY_COUNT, app.observability.AGENTK_DB_POOL_EXHAUSTED.
Existing app/llm.py: generate() opens the "chat" span and calls client.chat.completions.create(...);
keep record_llm_call_attributes + EmptyCompletionError behavior. Existing app/db.py: get_session()
is an async generator yielding AsyncSessionLocal(); AsyncSessionLocal/_engine are module singletons.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Retry-storm injector in generate (FLAG-03)</name>
  <files>app/llm.py, tests/test_injection_llm_db.py</files>
  <read_first>app/llm.py (generate() chat-span body, completion call, usage normalization, EmptyCompletionError), app/flags.py (is_enabled), app/observability.py (AGENTK_LLM_RETRY_COUNT), tests/conftest.py (mock_openai_client, in_memory_exporter, reset_flags)</read_first>
  <behavior>
    - retry_storm OFF: exactly one completion attempt; chat span AGENTK_LLM_RETRY_COUNT == 1; returns the answer normally (HTTP 200 contract preserved).
    - retry_storm ON with a provider that raises on the first N-1 attempts then succeeds: generate() retries up to MAX_RETRIES, eventually returns the answer, and the chat span AGENTK_LLM_RETRY_COUNT equals the number of attempts made (> 1); no exception propagates on eventual success.
    - retry_storm ON: the lowered timeout constant is passed to client.chat.completions.create (assert the create call received a reduced timeout kwarg on retry-storm attempts vs the normal timeout otherwise).
    - On genuine exhaustion (all MAX_RETRIES attempts raise) the last exception propagates (does not silently return an empty answer) — a distinct, tested edge.
  </behavior>
  <action>In app/llm.py generate(), inside the existing "chat" span, wrap client.chat.completions.create in a retry loop: read `flags.is_enabled("retry_storm")` fresh, choose RETRY_STORM_TIMEOUT_S (small) vs NORMAL_TIMEOUT_S and pass it as the `timeout=` kwarg to create(); count attempts; on exception, retry until MAX_RETRIES then re-raise; on success break. After the loop set span attribute AGENTK_LLM_RETRY_COUNT=attempts, then keep the existing usage-normalization, record_llm_call_attributes, empty-choices guard, and answer-extraction logic unchanged. Add module-level RETRY_STORM_TIMEOUT_S, NORMAL_TIMEOUT_S, MAX_RETRIES constants. Write tests/test_injection_llm_db.py::test_retry_storm using a mock client whose create() raises a transient error a fixed number of times before returning the conftest stub completion; assert attempt count on the span and that the call stays 200-equivalent (returns an LlmResult).</action>
  <verify>
    <automated>pytest tests/test_injection_llm_db.py::test_retry_storm -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/llm.py contains RETRY_STORM_TIMEOUT_S` and `flags.is_enabled("retry_storm")` and sets AGENTK_LLM_RETRY_COUNT on the chat span.
    - Test proves retry_storm ON yields attempts > 1 AND returns an LlmResult (no 5xx / no raised error on eventual success); OFF yields attempts == 1.
    - `pytest tests/test_injection_llm_db.py::test_retry_storm -q` exits 0.
  </acceptance_criteria>
</task>

<task type="auto" tdd="true">
  <name>Task 2: DB-pool-exhaustion injector via application-level limiter in get_session (FLAG-05)</name>
  <files>app/db.py, tests/test_injection_llm_db.py</files>
  <read_first>app/db.py (get_session async generator, AsyncSessionLocal, do-not-add-second-codec header note), app/flags.py (is_enabled), app/observability.py (AGENTK_DB_POOL_EXHAUSTED), 03-RESEARCH.md "Fault Injector 4" + Pitfall 3/4, tests/conftest.py (reset_flags)</read_first>
  <behavior>
    - db_pool_exhaustion OFF: get_session yields a session exactly as today; no semaphore acquisition; N concurrent checkouts all succeed.
    - db_pool_exhaustion ON: a module-level asyncio.Semaphore(1) gates checkout; a single checkout succeeds and releases on exit; when a second concurrent checkout is attempted while the first is held, asyncio.wait_for(acquire, small timeout) raises asyncio.TimeoutError, which is re-raised as a pool-exhaustion-shaped error (TimeoutError with a "pool exhaustion" message), logged at error level, and AGENTK_DB_POOL_EXHAUSTED flagged on the current span.
    - The real SQLAlchemy engine/pool is NOT reconstructed or resized (no create_async_engine change) — preserves no-restart guarantee and the single-codec rule.
  </behavior>
  <action>In app/db.py add a module-level `_pool_exhaustion_semaphore = asyncio.Semaphore(1)` and a small POOL_ACQUIRE_TIMEOUT_S constant. Rewrite get_session so that when `flags.is_enabled("db_pool_exhaustion")` is true it acquires the semaphore via asyncio.wait_for(...POOL_ACQUIRE_TIMEOUT_S); on asyncio.TimeoutError it logs an error via logging.getLogger(__name__), sets AGENTK_DB_POOL_EXHAUSTED=True on trace.get_current_span(), and raises TimeoutError("simulated DB pool exhaustion") from the timeout; on success it yields the session and releases the semaphore in a finally. When the flag is off, keep the original `async with AsyncSessionLocal() as session: yield session` path untouched. Do NOT register any pgvector connection-level codec. Write tests/test_injection_llm_db.py::test_db_pool_exhaustion that, with the flag on, holds one checkout and uses asyncio.gather of a second concurrent checkout to assert the pool-exhaustion TimeoutError is raised and logged; with the flag off, asserts concurrent checkouts succeed. Follow the tests/test_integration_rag.py engine.dispose()-between-loops pattern only if a real engine/session is exercised; prefer a lightweight AsyncSessionLocal monkeypatch so this stays a "not integration" unit test.</action>
  <verify>
    <automated>pytest tests/test_injection_llm_db.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `app/db.py contains _pool_exhaustion_semaphore` and `flags.is_enabled("db_pool_exhaustion")` and sets AGENTK_DB_POOL_EXHAUSTED on the current span.
    - `app/db.py` does NOT call create_async_engine a second time and adds no pgvector connect-level codec (grep confirms header rule intact).
    - Test proves a concurrent second checkout raises a pool-exhaustion TimeoutError (flag on) and both succeed (flag off).
    - `pytest tests/test_injection_llm_db.py -q` exits 0 (both scenarios).
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| flag state → llm.py / db.py injection points | Stuck-on flags spike LLM cost/call-rate or block DB checkouts on the monitored service |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-09 | Denial of Service | generate() retry loop | mitigate | Bounded by MAX_RETRIES; flag defaults OFF; only engaged on explicit retry_storm toggle |
| T-03-10 | Denial of Service | get_session semaphore limiter | mitigate | Application-level only, bounded acquire timeout; flag defaults OFF; real pool untouched so a stuck flag cannot corrupt the engine |
| T-03-11 | Information Disclosure | span attributes / logs | mitigate | Only counts/booleans (retry_count, pool_exhausted) and a non-sensitive error message logged; never prompt/answer text or API keys |
| T-03-12 | Spoofing | Cost SLO signal source | accept | AGENTK_LLM_RETRY_COUNT on the chat span is the queryable signal for the DASH-05 cost alert (trace-based); a dedicated metric is out of scope per research (no over-engineering) |
</threat_model>

<verification>
- `pytest tests/test_injection_llm_db.py -q` green (retry-storm attempt-count + eventual-success; concurrent pool exhaustion).
- Full offline suite `pytest tests/ -m "not integration" -q` still green (no regression to app/llm.py generate or app/db.py get_session healthy paths).
- (Deferred to HV-2) Toggle retry_storm against live SigNoz and confirm elevated retry_count with HTTP error rate flat; toggle db_pool_exhaustion under concurrent load and confirm pool-exhaustion errors correlate to failed traces.
</verification>

<success_criteria>
FLAG-03: retry_storm spikes attempts/cost signal while staying HTTP 200 (error rate not necessarily up).
FLAG-05: db_pool_exhaustion produces connection-pool-exhaustion errors in logs correlated to failed traces, live-toggleable with no restart.
</success_criteria>

<output>
Create `.planning/phases/03-failure-injection-dashboard-alerting/03-03-SUMMARY.md` when done.
</output>
