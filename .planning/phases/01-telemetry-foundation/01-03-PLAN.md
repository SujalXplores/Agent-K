---
phase: 01-telemetry-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified:
  - scripts/time-foundry-cast.sh
  - TELEMETRY-REBUILD-LOG.md
autonomous: false
requirements: [TELE-02, TELE-03]
must_haves:
  truths:
    - "scripts/time-foundry-cast.sh times `foundryctl cast` end-to-end and appends a timestamped duration row to TELEMETRY-REBUILD-LOG.md (D-08)"
    - "TELEMETRY-REBUILD-LOG.md contains at least one real MEASURED rebuild duration under 900 seconds (< 15 min), not an assumed value (TELE-02)"
    - "Traces from the /healthz request are visible in the SigNoz Traces Explorer (TELE-03)"
    - "Trace-correlated logs from the skeleton are visible in the SigNoz Logs Explorer (TELE-03)"
    - "At least one metric emitted by the skeleton is queryable/visible in the SigNoz UI (TELE-03)"
    - "The console exporter shows spans locally even if/when the SigNoz UI does not, so generation and delivery are debugged as independent variables (D-06)"
  artifacts:
    - path: "scripts/time-foundry-cast.sh"
      provides: "Rebuild-timing wrapper around foundryctl cast that appends to the log (D-08)"
      contains: "foundryctl cast"
    - path: "TELEMETRY-REBUILD-LOG.md"
      provides: "Append-only, judge-verifiable rebuild-duration log with a markdown table"
      contains: "Duration"
  key_links:
    - from: "app/main.py (/healthz, plan 01-01)"
      to: "SigNoz UI (plan 01-02)"
      via: "OTLP-HTTP export on port 4318 → SigNoz OTel Collector → ClickHouse → Traces/Logs Explorer"
      pattern: "4318"
    - from: "scripts/time-foundry-cast.sh"
      to: "TELEMETRY-REBUILD-LOG.md"
      via: "appends `| <timestamp> | <duration>s |` after timing foundryctl cast"
      pattern: "TELEMETRY-REBUILD-LOG"
---

<objective>
Close Phase 1 by (a) building and running the rebuild-timing harness that produces a real, measured, judge-verifiable rebuild duration under 15 minutes (TELE-02, D-08) and (b) verifying end-to-end that the skeleton's traces, metrics, and logs actually arrive in the SigNoz UI (TELE-03 verification half). This plan needs both prior Wave-1 outputs live: the FastAPI skeleton (01-01) to generate telemetry and the running SigNoz stack (01-02) to receive it.

Purpose: Turns "assumed" into "measured" for TELE-02, and proves the full traces/metrics/logs pipeline works before Phase 2 builds real app logic on top of it. The console-first debugging discipline (D-06) isolates generation from delivery if anything is missing.

Output: `scripts/time-foundry-cast.sh`, `TELEMETRY-REBUILD-LOG.md` (with at least one measured row).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/01-telemetry-foundation/01-CONTEXT.md
@.planning/phases/01-telemetry-foundation/01-RESEARCH.md
@.planning/phases/01-telemetry-foundation/01-PATTERNS.md
@.planning/phases/01-telemetry-foundation/01-02-SUMMARY.md
@.planning/phases/01-telemetry-foundation/01-01-SUMMARY.md
</context>

## Artifacts This Plan Produces

New files: `scripts/time-foundry-cast.sh`, `TELEMETRY-REBUILD-LOG.md`.

New artifacts/identifiers this plan introduces (exclude from cross-file drift verification — created here):
- Executable script `scripts/time-foundry-cast.sh` (times `foundryctl cast`, appends a markdown table row).
- `TELEMETRY-REBUILD-LOG.md` with header `| Timestamp (UTC) | Duration |` / `|---|---|` and >=1 measured data row.
- (Consumes, does not create: `app/main.py` /healthz from 01-01; the running SigNoz stack + `pours/deployment/compose.yaml` from 01-02.)

<tasks>

<task type="auto">
  <name>Task 1: Create rebuild-timing wrapper + initialize the rebuild log (D-08)</name>
  <files>scripts/time-foundry-cast.sh, TELEMETRY-REBUILD-LOG.md</files>
  <read_first>
    - .planning/phases/01-telemetry-foundation/01-PATTERNS.md (scripts/time-foundry-cast.sh section — the bash variant to use)
    - .planning/phases/01-telemetry-foundation/01-RESEARCH.md ("Code Examples" rebuild-time wrapper; Open Question #1 on what counts toward the 15-min budget)
    - casting.yaml (the file the script casts against)
  </read_first>
  <action>
    Create `scripts/time-foundry-cast.sh` (bash, per D-08 and RESEARCH — shell chosen over Python for simplicity): shebang for bash, `set -euo pipefail`, capture a start epoch via `date +%s`, run `foundryctl cast -f casting.yaml`, capture an end epoch, compute the integer second duration, compute a UTC ISO timestamp via `date -u`, append a row shaped `| <timestamp> | <duration>s |` to `TELEMETRY-REBUILD-LOG.md`, and echo a human-readable completion line. The timer wraps only the `foundryctl cast` invocation (not the one-time foundryctl binary install), per RESEARCH Open Question #1's budget boundary. Make the script executable (`chmod +x`).

    Initialize `TELEMETRY-REBUILD-LOG.md` with a title heading and a two-line markdown table header (a `| Timestamp (UTC) | Duration |` row followed by a `|---|---|` separator row) so the first script run appends a clean data row. Add a one-line note that the authoritative clean-machine measurement (fresh clone -> running SigNoz) is captured on a teammate's fresh clone at the Day 5-6 clean-machine-rebuild gate (per ROADMAP Phase 7 constraint); rows recorded here are same-machine cold-container measurements.
  </action>
  <verify>
    <automated>test -x scripts/time-foundry-cast.sh && grep -q 'foundryctl cast' scripts/time-foundry-cast.sh && grep -q 'TELEMETRY-REBUILD-LOG.md' scripts/time-foundry-cast.sh && test -f TELEMETRY-REBUILD-LOG.md && grep -q 'Duration' TELEMETRY-REBUILD-LOG.md && bash -n scripts/time-foundry-cast.sh</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/time-foundry-cast.sh` is executable, passes `bash -n` (syntax check), invokes `foundryctl cast`, and appends to `TELEMETRY-REBUILD-LOG.md`.
    - `TELEMETRY-REBUILD-LOG.md` exists with a markdown table header containing `Duration` and the deferred-clean-machine note.
  </acceptance_criteria>
  <done>The timing harness exists and is syntactically valid; the log is initialized and ready to receive measured duration rows.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify skeleton traces + logs + metrics are visible in the SigNoz UI (TELE-03)</name>
  <read_first>
    - .planning/phases/01-telemetry-foundation/01-RESEARCH.md (Pitfall 5 "Last 30 minutes" default window; Validation Architecture "Manual-Only Verifications" step list)
    - .planning/phases/01-telemetry-foundation/01-VALIDATION.md (the TELE-03 manual verification instructions)
    - app/main.py, app/telemetry.py (the skeleton + exporter wiring from 01-01)
  </read_first>
  <action>
    Automate everything before the pause: start the skeleton with `uvicorn app.main:app` (port 8000), curl `localhost:8000/healthz` several times to generate fresh traffic, and confirm at least one span printed to console stdout (proving generation locally, D-06). THEN pause for the developer to verify traces, trace-correlated logs, and at least one metric are visible in the SigNoz UI at `localhost:8080` per the steps below. If a signal is missing, widen the default "Last 30 minutes" window and re-generate traffic before concluding OTLP delivery is broken (Pitfall 5).
  </action>
  <what-built>
    The full pipeline is now live: the FastAPI skeleton (01-01) exports to OTLP-HTTP :4318, and the SigNoz stack (01-02) is running to receive it. Before this checkpoint, the executor has started the skeleton (`uvicorn app.main:app`), generated fresh traffic (`curl -sf localhost:8000/healthz` a few times), and confirmed the console exporter printed a span to stdout — so span GENERATION is already proven locally (D-06). This checkpoint confirms DELIVERY into SigNoz, which is inherently a human UI check (no SigNoz query API client exists until Phase 4). This is the automated-verification boundary for TELE-03 — it is manual by the requirement's own wording ("confirmed visible in the SigNoz UI").
  </what-built>
  <how-to-verify>
    1. Ensure the skeleton is running and you have curled `localhost:8000/healthz` in the last minute (generate fresh traffic).
    2. Open the SigNoz UI at `http://localhost:8080`.
    3. Traces: open Traces Explorer, find a span from service `agent-k-rag-service` for the `/healthz` request.
    4. Logs: open Logs Explorer, find the skeleton's startup/request log line carrying a trace_id/span_id (trace-correlated).
    5. Metrics: open a metrics query/panel and confirm at least one metric from the service is present.
    6. If any panel looks empty, widen the default "Last 30 minutes" time window and re-generate traffic BEFORE concluding delivery is broken (Pitfall 5). If console stdout shows spans but SigNoz (widened window) does not, that isolates a real OTLP delivery problem to debug.
  </how-to-verify>
  <acceptance_criteria>
    - A `/healthz` trace from `agent-k-rag-service` is visible in SigNoz Traces Explorer.
    - A trace-correlated log line is visible in SigNoz Logs Explorer.
    - At least one metric from the skeleton is visible/queryable in the SigNoz UI.
    - (Fallback signal, if delivery fails) console stdout shows the span, proving generation is fine and isolating the issue to delivery.
  </acceptance_criteria>
  <resume-signal>Type "telemetry confirmed" once traces, logs, and a metric are visible in SigNoz, or describe which signal is missing (and whether console showed the span).</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record a measured rebuild duration under 15 minutes (TELE-02)</name>
  <files>TELEMETRY-REBUILD-LOG.md</files>
  <read_first>
    - scripts/time-foundry-cast.sh (the wrapper created in Task 1)
    - SIGNOZ-RUNBOOK.md (teardown/standup commands from 01-02)
    - .planning/phases/01-telemetry-foundation/01-RESEARCH.md (Open Question #1 — measurement boundary and the 900s budget)
  </read_first>
  <action>
    Produce a real, representative cold-container measurement (this runs AFTER the Task 2 UI verification, so the stack was already confirmed working). First tear the stack down so `cast` measures a genuine bring-up rather than a warm no-op: run `docker compose -f pours/deployment/compose.yaml down` (the runbook documents this path). Then run `scripts/time-foundry-cast.sh`, which times `foundryctl cast -f casting.yaml` and appends a `| <timestamp> | <duration>s |` row to `TELEMETRY-REBUILD-LOG.md`. Confirm the appended duration is under 900 seconds. If the first run exceeds 900s, the largest likely cost is one-time Docker image pulls (already cached now) — re-run once and record; if it still exceeds, note the actual number honestly in the log rather than discarding it (TELE-02 is "measured, not assumed"). Do NOT commit the regenerated `pours/`; commit only `scripts/time-foundry-cast.sh` and `TELEMETRY-REBUILD-LOG.md` via scoped `git add`.
  </action>
  <verify>
    <automated>bash scripts/time-foundry-cast.sh && LAST=$(grep -oE '\| [0-9]+s \|' TELEMETRY-REBUILD-LOG.md | tail -1 | grep -oE '[0-9]+') && test -n "$LAST" && [ "$LAST" -lt 900 ]</automated>
  </verify>
  <acceptance_criteria>
    - `TELEMETRY-REBUILD-LOG.md` gains at least one measured data row with a duration value.
    - The most recent recorded duration is under 900 seconds (< 15 min), satisfying TELE-02.
    - After the run the SigNoz stack is back up (`cast` re-deployed it); `pours/` remains untracked.
  </acceptance_criteria>
  <done>A real, judge-verifiable rebuild duration under 15 minutes is recorded in TELEMETRY-REBUILD-LOG.md — TELE-02 satisfied on this machine, with the definitive fresh-clone measurement deferred to the Day 5-6 clean-machine gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| timing wrapper script → shell | The script spawns `foundryctl cast` / `docker compose` subprocesses on the dev machine |
| skeleton → SigNoz Collector | Telemetry (traces/metrics/logs) crosses from the app process to the observability backend over OTLP-HTTP :4318 |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Tampering | `scripts/time-foundry-cast.sh` subprocess execution | low | accept | Script takes NO external/untrusted input; it runs fixed `foundryctl cast` / `docker compose` commands with a static casting.yaml. No injection surface. |
| T-01-02 | Information Disclosure | OTLP ingest / UI ports during verification | medium | mitigate | Verification traffic stays on localhost (`localhost:8000`, `localhost:8080`); re-confirm bindings are not exposed publicly per SIGNOZ-RUNBOOK.md (inherited from plan 01-02). Below the "high" block threshold. |
</threat_model>

<verification>
- `bash -n scripts/time-foundry-cast.sh` passes; the script is executable and casts against `casting.yaml`.
- `TELEMETRY-REBUILD-LOG.md` has a table header and >=1 measured row with a duration < 900s.
- SigNoz UI shows: a `/healthz` trace, a trace-correlated log line, and at least one skeleton metric (human-verified, Task 2).
- Console exporter shows the span locally regardless of SigNoz delivery (D-06 debugging discipline).
</verification>

<success_criteria>
Phase 1 is provably complete: a measured (not assumed) rebuild duration under 15 minutes is logged (TELE-02), and the skeleton's traces, metrics, and logs are confirmed visible in the SigNoz UI (TELE-03) — with the console-exporter fallback demonstrated as the first debugging step.
</success_criteria>

<output>
Create `.planning/phases/01-telemetry-foundation/01-03-SUMMARY.md` when done.
</output>
