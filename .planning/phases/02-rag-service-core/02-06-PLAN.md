---
phase: 02-rag-service-core
plan: 06
type: execute
wave: 5
depends_on: [02-05]
gap_closure: true
files_modified:
  - app/main.py
  - scripts/probe_ask_spans.py
  - tests/test_integration_rag.py
  - tests/test_ask.py
autonomous: true
requirements: [RAG-01, RAG-03]
must_haves:
  truths:
    - "The async SQLAlchemy engine is registered with opentelemetry-instrumentation-sqlalchemy so retrieval queries emit free DB spans (02-01 must-have, D-07)"
    - "A real POST /ask emits all three GenAI spans — rag.retrieval, rag.prompt_construction, and chat — with their rag.* / gen_ai.* attributes (ROADMAP SC-3, RAG-03, D-07)"
    - "A SQLAlchemy DB span for the retrieval SELECT is emitted beneath the hand-written rag.retrieval span, proven by measurement rather than by the existence of an instrumentation function"
    - "The D-07 span assertion in tests/test_ask.py measures something true of production instead of passing because framework spans never reached a partially-bound provider"
  artifacts:
    - path: "app/main.py"
      provides: "Startup wiring that calls setup_db_instrumentation(), preserving the numbered-step ordering and the routes-registered-before-instrumentation invariant"
      contains: "setup_db_instrumentation"
    - path: "scripts/probe_ask_spans.py"
      provides: "Production span probe — binds an in-memory exporter as the global TracerProvider BEFORE importing app.main, issues a real POST /ask against the live database with the provider stubbed, and writes the full observed span set to a JSON file"
      exports: ["main"]
    - path: "tests/test_integration_rag.py"
      provides: "An integration test that runs the probe and asserts a SQLAlchemy SELECT span and all three GenAI spans are present in the production span set"
      contains: "probe_ask_spans"
    - path: "tests/test_ask.py"
      provides: "Span assertions scoped by instrumentation scope (app.rag / app.llm) rather than a total span count that does not reflect production"
      contains: "instrumentation_scope"
  key_links:
    - from: "app/main.py"
      to: "app/db.py"
      via: "startup calls setup_db_instrumentation(), which runs SQLAlchemyInstrumentor().instrument(engine=get_engine().sync_engine)"
      pattern: "setup_db_instrumentation"
    - from: "scripts/probe_ask_spans.py"
      to: "app/main.py"
      via: "probe sets the global TracerProvider before importing app.main so framework and DB instrumentation bind to the observable provider"
      pattern: "set_tracer_provider"
    - from: "tests/test_integration_rag.py"
      to: "scripts/probe_ask_spans.py"
      via: "subprocess invocation, then JSON assertions on span names and instrumentation scopes"
      pattern: "subprocess"
---

<objective>
Close the two remaining SC-3 / RAG-03 gaps: wire the orphaned `setup_db_instrumentation()` into application startup so retrieval queries actually emit the free DB span D-07 was designed around, and replace the `tests/test_ask.py` span assertion that passes for an accidental reason with one that measures the production span set.

Purpose: `app/db.py` defines `setup_db_instrumentation()` and nothing in the repo calls it — a healthy `/ask` emits 7 spans and zero DB spans as shipped, rising to 9 (`connect` + `SELECT`) when the function is invoked. Meanwhile `tests/test_ask.py:48` asserts a total of 3 spans, which only passes because `FastAPIInstrumentor` binds its tracer at import time and its 4 framework spans never reach the monkeypatched exporter. One is missing wiring; the other is a test that cannot see production. Both are fixed here, and both are proven by a probe that measures the real span set instead of asserting a function exists.

Output: a one-line startup wiring change in `app/main.py`, a new `scripts/probe_ask_spans.py` production span probe, an integration test asserting the DB span and the three GenAI spans, and a corrected assertion in `tests/test_ask.py`. Depends on 02-05 (without the pgvector fix, `/ask` returns 500 and neither the prompt-construction nor the chat span is ever created). Wave 5.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/02-rag-service-core/02-CONTEXT.md
@.planning/phases/02-rag-service-core/02-VERIFICATION.md
@.planning/phases/02-rag-service-core/02-PATTERNS.md
@app/main.py
@app/db.py
@app/rag.py
@app/llm.py
@app/telemetry.py
@app/observability.py
@tests/conftest.py
@tests/test_ask.py

<interfaces>
app/main.py's load-bearing ordering convention (stated in the file's own docstring and comments,
and in 02-PATTERNS.md "Route Registration Ordering") — preserve it exactly:
  1. Create the app.
  2. Register routes (/healthz, /ask).            <- routes MUST stay before step 4
  3. Wire OTel providers -> setup_telemetry()
  4. Instrument the app AFTER routes are registered -> FastAPIInstrumentor.instrument_app(app)
  5. Correlate stdlib logging -> LoggingInstrumentor().instrument(set_logging_format=True)
Add the DB instrumentation call as a new numbered step AFTER setup_telemetry() (so the instrumentor
binds to the real provider) and renumber the following comments. Do not move the /ask route.

Provider-binding mechanics verified live during planning (this is why a subprocess probe is required):
- opentelemetry.trace.set_tracer_provider() is do-once per process. If a TracerProvider is registered
  BEFORE `import app.main`, setup_telemetry()'s own set_tracer_provider() call is refused (warning only)
  and the pre-registered provider stays active. Confirmed: provider_ours=True after importing app.main.
- With that pre-binding, FastAPI framework spans DO reach the exporter. A GET /healthz produced 3 spans,
  all with instrumentation_scope.name == "opentelemetry.instrumentation.fastapi"
  ("GET /healthz", plus two "GET /healthz http send"). A POST adds an "http receive" span, which is why
  the verifier measured 7 spans on a healthy /ask (4 framework + 3 application).
- setup_db_instrumentation() then adds SQLAlchemy spans with
  instrumentation_scope.name == "opentelemetry.instrumentation.sqlalchemy". Confirmed live: a session
  executing a statement produced spans named "connect" and "select" under that scope (SQLAlchemy names
  the span after the leading SQL keyword, so an ORM select compiles to a name beginning with "SELECT").
- tests/conftest.py's in_memory_exporter fixture monkeypatches trace._TRACER_PROVIDER AFTER app.main has
  already been imported, so framework and SQLAlchemy spans can never reach it. That is exactly why the
  in-process test must assert on application-scoped spans only, and why the production count must be
  measured by a separate process.
- Application span scopes confirmed live: app/rag.py spans -> "app.rag", app/llm.py "chat" span -> "app.llm".

IMPORTANT: importing app.main runs setup_telemetry(), which installs a ConsoleSpanExporter. The probe
must therefore write its results to a FILE, not to stdout — stdout is flooded with exported telemetry JSON.

Existing symbols (do not redefine):
- app/db.py:  setup_db_instrumentation(), get_engine(), AsyncSessionLocal, get_session()
- app/main.py: app, healthz(), ask()
- tests/test_integration_rag.py (from 02-05): pytestmark = pytest.mark.integration, _live_corpus_doc_count(),
  live_session, live_client, and its two tests — ADD to this module, do not rewrite it.
- tests/conftest.py: in_memory_exporter, mock_openai_client, stub_documents, fake_session, client,
  pytest_configure

Environment: rag-postgres is already running, healthy, and seeded with 72 docs — connect to it only.
No task may stop, restart, kill, or tear down any container or process. Use the /tmp/agentk-p2
virtualenv (it carries greenlet==3.5.4; the repo-local .venv does not).
</interfaces>
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02-06)

This is gap closure, so most touched symbols already exist. Genuinely NEW:

New files:
- `scripts/probe_ask_spans.py` — production span probe

New symbols:
- `scripts/probe_ask_spans.py`: `main(output_path)` entrypoint; internal helpers `_install_observable_provider()`, `_stub_llm_provider()`; JSON output schema `{"http_status": int, "span_count": int, "spans": [{"name": str, "scope": str, "status": str, "attributes": [str, ...]}]}`
- `tests/test_integration_rag.py`: new test `test_ask_emits_db_span_and_three_genai_spans_in_production`
- `app/main.py`: a new numbered startup step invoking the already-existing `setup_db_instrumentation()` (no new function is defined)

Modified (assertions corrected, nothing removed):
- `tests/test_ask.py`: `test_ask_produces_exactly_three_genai_spans` — total-count assertion replaced with an instrumentation-scope-filtered assertion; all existing per-name and per-attribute assertions retained
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Call setup_db_instrumentation() at application startup in app/main.py</name>
  <files>app/main.py</files>
  <read_first>
    - app/main.py (the whole 75-line file — note the numbered comment steps 1-5 at lines 24, 28, 65, 68, 71, and the docstring at lines 1-8 that states routes must be registered before instrumentation)
    - app/db.py (as corrected by 02-05 — read `setup_db_instrumentation()`'s docstring: it must be called once at application startup, after `get_engine()` has built the engine)
    - app/telemetry.py (`setup_telemetry()` registers the global TracerProvider — the DB instrumentor must be wired after it so it binds to a real provider)
    - .planning/phases/02-rag-service-core/02-VERIFICATION.md (gap 3: `reason`, `artifacts`, and `missing` — including the measured 7-spans/0-DB-spans vs 9-spans evidence)
    - .planning/phases/02-rag-service-core/02-PATTERNS.md section "Route Registration Ordering"
  </read_first>
  <action>
    Import `setup_db_instrumentation` from `app.db` in `app/main.py` alongside the existing `get_session` import, and call it once at module level during startup.

    Placement: insert it as a new numbered step between the existing step 4 (`FastAPIInstrumentor.instrument_app(app)`, line 69) and the existing step 5 (`LoggingInstrumentor().instrument(set_logging_format=True)`, line 72), then renumber the logging step and the trailing comment sequence accordingly. It must come after `setup_telemetry()` so `SQLAlchemyInstrumentor` binds to the real, already-registered TracerProvider rather than to a placeholder. It must NOT be moved above the `/ask` route definition — the file's documented invariant is that routes are registered before instrumentation is wired, and nothing in this task may disturb that.

    Give the new step a comment in the same voice as its neighbours, stating that it instruments the sync core beneath the async engine so every retrieval query emits a free DB span underneath the hand-written `rag.retrieval` span (D-07), and that without this call `SQLAlchemyInstrumentor` never activates and the free DB span is silently absent (02-VERIFICATION.md gap 3).

    Do not change the `/ask` handler, the `/healthz` handler, `setup_telemetry()`, or `FastAPIInstrumentor.instrument_app(app)`. Do not add a FastAPI lifespan/startup event — this file wires infrastructure at module level and that shape must be preserved.
  </action>
  <verify>
    <automated>. /tmp/agentk-p2/bin/activate 2>/dev/null; python -c "src=open('app/main.py').read(); i=src.index('/ask'); j=src.index('instrument_app'); k=src.index('setup_db_instrumentation()'); t=src.index('setup_telemetry()'); assert i<j, 'FAIL: /ask registered after instrument_app'; assert t<k, 'FAIL: DB instrumentation wired before setup_telemetry()'; print('ordering OK: /ask < instrument_app, setup_telemetry < setup_db_instrumentation')"</automated>
    <automated>. /tmp/agentk-p2/bin/activate 2>/dev/null; python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - SOURCE: `app/main.py` imports `setup_db_instrumentation` from `app.db` and invokes it exactly once at module level.
    - SOURCE (ordering): in `app/main.py`, the `/ask` route literal appears before `instrument_app`, and `setup_telemetry()` appears before the `setup_db_instrumentation()` call — asserted by the automated index check, not by inspection.
    - SOURCE: the numbered startup comments remain a contiguous, correctly ordered sequence with the new DB-instrumentation step inserted and the following steps renumbered.
    - BEHAVIOR: importing `app.main` still succeeds with no database connection required (instrumentation attaches listeners to the engine; it does not connect) — `python -m pytest tests/ -q` still passes with the existing mocked tests untouched.
    - BEHAVIOR (proven in Task 2): a real `/ask` now emits SQLAlchemy-scoped spans. Task 1 alone is not considered done until Task 2's probe shows a `SELECT` span.
  </acceptance_criteria>
  <done>`setup_db_instrumentation()` is no longer orphaned — it runs at startup, after the tracer provider is registered and after routes are registered, so retrieval queries emit the free DB span D-07 specifies.</done>
</task>

<task type="auto">
  <name>Task 2: Add a production span probe and an integration test asserting the DB span and the three GenAI spans</name>
  <files>scripts/probe_ask_spans.py, tests/test_integration_rag.py</files>
  <read_first>
    - app/main.py (as modified by Task 1 — the probe must import this module AFTER installing its own provider)
    - app/telemetry.py (confirm `setup_telemetry()` calls `trace.set_tracer_provider(...)` and adds a ConsoleSpanExporter — this is why the probe writes to a file rather than stdout)
    - tests/test_integration_rag.py (created in 02-05 — read its module docstring, `pytestmark`, `_live_corpus_doc_count()` skip guard, `live_session`, and `live_client`; you are APPENDING one test, not rewriting the module)
    - tests/test_ask.py (the `_wire_offline` helper's provider-stub shape — the probe needs an equivalent stub, but must NOT stub `embed_text`, because the real embedding is what exercises the pgvector path and therefore the DB span)
    - tests/conftest.py (`mock_openai_client` shows the exact response shape `app.llm.generate()` reads: `.model`, `.usage` with prompt/completion or input/output token counts, and `.choices[0].message.content`)
    - .planning/phases/02-rag-service-core/02-VERIFICATION.md (gap 2 and gap 3 `missing` items — specifically "a verification step that asserts a DB span is emitted for the retrieval query, not just that the instrumentation function exists")
  </read_first>
  <action>
    Create `scripts/probe_ask_spans.py`, a standalone production span probe. Its module docstring must explain that it exists because `tests/conftest.py`'s `in_memory_exporter` fixture monkeypatches the tracer provider AFTER `app.main` has been imported, so FastAPI and SQLAlchemy instrumentation — which bind their tracers at instrument time — can never be observed from inside the normal test process. Measuring the real span set requires a fresh process that registers an observable provider first.

    Probe behavior, in this order:
    1. Read a required output file path from `sys.argv[1]`; exit non-zero with a usage message if absent.
    2. Build an `InMemorySpanExporter`, attach it to a `TracerProvider` via a `SimpleSpanProcessor`, and register that provider with `trace.set_tracer_provider(...)` BEFORE importing `app.main`. Assert afterwards that `trace.get_tracer_provider()` is still the probe's provider, and fail loudly if it is not — silently measuring the wrong provider is the exact failure mode this probe exists to prevent.
    3. Ensure a placeholder `GROQ_API_KEY` is present in the environment (02-07 makes a missing provider key a hard failure) and stub the third-party call by replacing the `OpenAI` name in the `app.llm` module with a factory that returns a stub client. The stub's `chat.completions.create(...)` must return an object exposing `.model`, `.usage` with prompt/completion token counts, and a non-empty `.choices` list whose first element has `.message.content` set to a fixed placeholder string. Use `types.SimpleNamespace` for the stub. Do NOT stub the database, and do NOT stub `app.rag.embed_text` — the real 384-dim local embedding is the bound parameter that exercises the pgvector query and therefore the DB span.
    4. Import `app.main`, then issue one `POST /ask` through `fastapi.testclient.TestClient` with a fixed question (`"How do I rotate an API key?"`) and no `dependency_overrides`, so the request hits the live `rag-postgres` database.
    5. Collect `exporter.get_finished_spans()` and write JSON to the argv path with keys `http_status`, `span_count`, and `spans` — each span entry carrying `name`, `scope` (from `span.instrumentation_scope.name`), `status` (the status code name), and `attributes` (the sorted list of attribute KEYS only). Record attribute keys, never attribute values: values can contain model names and token counts, and this file must stay safe to commit and to paste into an issue (T-02-KEY).
    6. Exit 0 on success.

    Then append one test to `tests/test_integration_rag.py`, named `test_ask_emits_db_span_and_three_genai_spans_in_production`. It runs the probe with `subprocess.run([sys.executable, "scripts/probe_ask_spans.py", str(tmp_path / "spans.json")], cwd=<repo root>, capture_output=True)`, asserts `returncode == 0` (including the probe's stderr in the assertion message on failure), loads the JSON, and asserts:
    - `http_status == 200` — the endpoint served a real request against the live database.
    - Exactly one span named `rag.retrieval` and exactly one named `rag.prompt_construction`, both with scope `app.rag`; exactly one named `chat` with scope `app.llm`. These are the three D-07 GenAI spans, now measured against a correctly bound provider.
    - `rag.retrieval.top_k` and `rag.retrieval.doc_count` are among the `rag.retrieval` span's attribute keys; `gen_ai.request.model` and `gen_ai.usage.input_tokens` are among the `chat` span's attribute keys.
    - At least one span with scope `opentelemetry.instrumentation.sqlalchemy` whose name upper-cased starts with `SELECT`. This is the D-07 free DB span and is the assertion that fails if Task 1's wiring is removed. Assert on the scope rather than only the name, since SQLAlchemy derives the span name from the leading SQL keyword.
    - At least one span with scope `opentelemetry.instrumentation.fastapi` — evidence that the probe genuinely measured the framework layer, which the in-process suite cannot see.
    - `span_count >= 8`, with an inline comment recording the measured baselines from 02-VERIFICATION.md: 7 spans on the healthy path without DB instrumentation, 9 with it. Use a lower bound rather than an exact number so the test does not become brittle to connection-pool reuse.

    Do not modify 02-05's two tests or the module's skip guard — the new test inherits the module-level `integration` marker and the live-corpus skip automatically.
  </action>
  <verify>
    <automated>. /tmp/agentk-p2/bin/activate 2>/dev/null; python -m pytest tests/test_integration_rag.py -q -m integration</automated>
    <automated>. /tmp/agentk-p2/bin/activate 2>/dev/null; python scripts/probe_ask_spans.py /tmp/agentk-probe-spans.json >/dev/null 2>&1; python -c "import json; d=json.load(open('/tmp/agentk-probe-spans.json')); names=[s['name'] for s in d['spans']]; scopes=[s['scope'] for s in d['spans']]; assert d['http_status']==200, d['http_status']; assert names.count('rag.retrieval')==1 and names.count('rag.prompt_construction')==1 and names.count('chat')==1, names; assert any(sc=='opentelemetry.instrumentation.sqlalchemy' and s['name'].upper().startswith('SELECT') for s,sc in zip(d['spans'],scopes)), names; print('production span set OK:', d['span_count'], 'spans ->', names)"</automated>
    <automated>. /tmp/agentk-p2/bin/activate 2>/dev/null; python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR: `python scripts/probe_ask_spans.py <path>` exits 0 and writes JSON in which `http_status == 200` for a real `POST /ask` against the live seeded database.
    - BEHAVIOR: the probe's span set contains at least one span with scope `opentelemetry.instrumentation.sqlalchemy` whose name starts with `SELECT` — the D-07 free DB span, measured rather than inferred. Reverting Task 1's wiring makes this assertion fail.
    - BEHAVIOR: the probe's span set contains exactly one `rag.retrieval` and one `rag.prompt_construction` (scope `app.rag`) and one `chat` (scope `app.llm`), each carrying its expected attribute keys — all three GenAI spans reachable on a real request, which is impossible today because retrieval raises first.
    - BEHAVIOR: the probe's span set contains at least one `opentelemetry.instrumentation.fastapi` span, and `span_count >= 8` — confirming the measurement reflects the production span set (7 without DB instrumentation, 9 with it) rather than the 3 the old in-process assertion reported.
    - BEHAVIOR: `python -m pytest tests/test_integration_rag.py -q -m integration` reports 3 passed (02-05's two plus this one).
    - SECURITY: the probe's JSON output records attribute KEYS only — no attribute values, no question text, no prompt or answer text, and no API key value (T-02-KEY).
    - REGRESSION: `python -m pytest tests/ -q` passes; 02-05's tests and all pre-existing mocked tests are unmodified.
  </acceptance_criteria>
  <done>The free DB span required by D-07 is proven to be emitted for the retrieval query, all three GenAI spans are proven present on a real request, and the project has a reusable probe for measuring the production span set.</done>
</task>

<task type="auto">
  <name>Task 3: Re-derive the misleading total-span-count assertion in tests/test_ask.py</name>
  <files>tests/test_ask.py</files>
  <read_first>
    - tests/test_ask.py (the whole file — focus on `test_ask_produces_exactly_three_genai_spans` at lines 35-58 and the module docstring at lines 1-6)
    - tests/conftest.py (`in_memory_exporter`'s docstring explains that it monkeypatches `trace._TRACER_PROVIDER`, and why that only works for modules acquiring a tracer per call — this is the root of the accidental pass)
    - scripts/probe_ask_spans.py (from Task 2 — the correct place to measure the production span count; reference it in the comment you add)
    - .planning/phases/02-rag-service-core/02-VERIFICATION.md (gap 2 `artifacts` entry for tests/test_ask.py, and the Review-Lead Adjudication paragraph on WR-16)
  </read_first>
  <action>
    In `tests/test_ask.py::test_ask_produces_exactly_three_genai_spans`, keep every existing assertion — the three `span_names.count(...) == 1` checks and all five attribute-presence checks are correct and must remain. Change only the total-span-count assertion at line 48.

    Replace it with an assertion scoped by instrumentation scope: build a list of the finished spans whose `instrumentation_scope.name` is `app.rag` or `app.llm`, assert that list has exactly 3 entries, and assert the set of their names equals the three expected names. Application spans are the only ones this fixture can observe, and this phrasing states that fact deliberately instead of relying on it accidentally.

    Add a comment on that assertion recording why the old form was wrong and what the real numbers are: `FastAPIInstrumentor` binds its tracer at `instrument_app()` time, which happens when `app.main` is imported — before the `in_memory_exporter` fixture monkeypatches the provider — so its 4 framework spans and the SQLAlchemy DB spans never reach this exporter. A real `/ask` emits 7 spans without DB instrumentation and 9 with it, so any total-count assertion in this fixture is meaningless. Point the reader at `scripts/probe_ask_spans.py` and `tests/test_integration_rag.py` as the place where the production span set is actually measured.

    Update the module docstring, which currently claims coverage of "exactly three GenAI-instrumented spans per call", to say that this module covers the three application-scoped GenAI spans under a mocked database and provider, and that the production span set — including the FastAPI framework spans and the SQLAlchemy DB span — is measured by the integration module instead.

    Do not change any other test in the file, do not change the test's name, and do not touch `tests/conftest.py`.
  </action>
  <verify>
    <automated>. /tmp/agentk-p2/bin/activate 2>/dev/null; python -m pytest tests/test_ask.py -q</automated>
    <automated>. /tmp/agentk-p2/bin/activate 2>/dev/null; python -c "src=open('tests/test_ask.py').read(); assert 'instrumentation_scope' in src, 'span assertion is not scope-filtered'; assert src.count('span_names.count(') >= 3, 'per-name span assertions were removed'; assert 'probe_ask_spans' in src, 'no pointer to the production span measurement'; print('test_ask.py assertion shape OK')"</automated>
    <automated>. /tmp/agentk-p2/bin/activate 2>/dev/null; python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - BEHAVIOR: `python -m pytest tests/test_ask.py -q` passes with all 5 tests, and `test_ask_produces_exactly_three_genai_spans` now asserts on spans filtered by `instrumentation_scope.name` in `app.rag` / `app.llm`.
    - BEHAVIOR: the test would fail if a fourth application span were added or one of the three were dropped — the assertion is on an exact count of application-scoped spans plus an exact name set, not on a total that the fixture cannot observe.
    - SOURCE: all three `span_names.count(...) == 1` assertions and all five attribute-presence assertions from the original test are still present.
    - SOURCE: `tests/test_ask.py` references `scripts/probe_ask_spans.py` so a future reader is directed to where the production span set is measured, and its module docstring no longer claims to prove the production three-span total.
    - REGRESSION: `python -m pytest tests/ -q` passes with no test deleted, renamed, or skipped.
  </acceptance_criteria>
  <done>The D-07 span assertion in the mocked suite states exactly what it can prove (three application-scoped GenAI spans) and points at the probe that proves the rest, so it can no longer pass for an accidental reason.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| RAG app -> OTel collector / SigNoz | Span names and attributes leave the process and are persisted in a telemetry backend |
| probe process -> live rag-postgres | The probe opens a real database connection and issues a real retrieval query |
| probe JSON artifact -> repo / issue tracker | The probe's output file may be shared while debugging |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02G-05 | Information Disclosure | SQLAlchemy DB spans now exported for every retrieval query | medium | mitigate | `opentelemetry-instrumentation-sqlalchemy` records the compiled SQL statement text, not bound parameter values, so the exported `db.statement` contains the parameterized cosine-distance query and never the user's question or the embedding vector. No `enable_commenter` or parameter-capture option is enabled. Re-affirms T-02-KEY from 02-03/02-04. |
| T-02G-06 | Information Disclosure | `scripts/probe_ask_spans.py` JSON output | medium | mitigate | The probe records span attribute KEYS only, never attribute values, and never the question, prompt, answer, or API key. The provider is stubbed and the key env var is a visible placeholder, so no real credential can be captured. |
| T-02G-07 | Repudiation | Test assertions that pass for reasons unrelated to production behavior | medium | mitigate | The in-process assertion is narrowed to what the fixture can genuinely observe (application-scoped spans) and annotated with the real production counts; the production span set is separately measured by a probe running in a fresh process with a correctly bound provider. A green suite can no longer coexist with an unobservable pipeline. |
| T-02G-08 | Denial of Service | Extra DB spans on every query in the hot path | low | accept | One `connect` span per new pooled connection plus one `SELECT` span per retrieval is negligible at hackathon volume, and this instrumentation is an explicit D-07 requirement and a judged deliverable. Sampling/volume tuning is out of scope for this milestone. |
| T-02-SC | Tampering | npm/pip/cargo installs | high | mitigate | This plan installs no packages. `opentelemetry-instrumentation-sqlalchemy==0.65b0` is already pinned in `requirements.txt` and was PyPI-verified during 02-01; the probe uses only already-installed `opentelemetry-sdk`, `fastapi`, and stdlib modules. No `[ASSUMED]`/`[SUS]` package legitimacy checkpoint is required. |
</threat_model>

<verification>
Automated (run by the executor against the already-running `rag-postgres` container):
- `app/main.py` ordering check: `/ask` before `instrument_app`, and `setup_telemetry()` before `setup_db_instrumentation()`.
- `python scripts/probe_ask_spans.py <path>` exits 0; the JSON shows `http_status == 200`, one each of `rag.retrieval` / `rag.prompt_construction` / `chat`, at least one `opentelemetry.instrumentation.sqlalchemy` span whose name starts with `SELECT`, at least one `opentelemetry.instrumentation.fastapi` span, and `span_count >= 8`.
- `python -m pytest tests/test_integration_rag.py -q -m integration` → 3 passed.
- `python -m pytest tests/ -q` → all pass, nothing skipped that was passing before.

Do NOT run any command that stops, restarts, recreates, or removes a container or process.

End-of-phase human check (carried into 02-07, where both human-verification items are collected):
<human-check>The "in SigNoz" half of SC-3 cannot be closed by any automated check in this plan — the probe proves the spans are produced, not that they are ingested and rendered. See 02-07's human-verification section.</human-check>
</verification>

<success_criteria>
- The D-07 free DB span is emitted for the retrieval query and proven by measurement, closing the "async engine registered with opentelemetry-instrumentation-sqlalchemy" must-have.
- All three GenAI spans (`rag.retrieval`, `rag.prompt_construction`, `chat`) are present with their `rag.*` / `gen_ai.*` attributes on a real `POST /ask`, closing the producible half of SC-3 / RAG-03.
- The D-07 assertion in the mocked suite measures something true and points at the probe for the rest — the class of defect that hid this gap can no longer recur silently.
- `POST /ask` continues to return 200 against the live database (RAG-01 regression protection).
</success_criteria>

<output>
Create `.planning/phases/02-rag-service-core/02-06-SUMMARY.md` when done.
</output>
