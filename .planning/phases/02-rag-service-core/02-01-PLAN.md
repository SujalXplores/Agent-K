---
phase: 02-rag-service-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - docker-compose.yaml
  - .env.example
  - app/embeddings.py
  - app/db.py
  - app/models.py
  - alembic.ini
  - alembic/env.py
  - alembic/script.py.mako
  - alembic/versions/0001_create_documents.py
autonomous: true
requirements: [RAG-01, RAG-02]
user_setup: []
must_haves:
  truths:
    - "A pgvector-enabled Postgres container is running and reachable at DATABASE_URL"
    - "The documents table exists in the live database with the pgvector extension enabled and a vector(384) embedding column"
    - "The embedding module produces 384-dim vectors from a local sentence-transformers model with zero external API calls"
    - "The async SQLAlchemy engine is registered with opentelemetry-instrumentation-sqlalchemy so retrieval queries emit free DB spans (D-07)"
  artifacts:
    - path: "docker-compose.yaml"
      provides: "pgvector/pgvector:pg16 Postgres service for the RAG datastore"
      contains: "pgvector/pgvector"
    - path: "app/embeddings.py"
      provides: "Local sentence-transformers embedding helper + EMBEDDING_DIM constant"
      exports: ["EMBEDDING_DIM", "get_model", "embed_text", "embed_texts"]
    - path: "app/db.py"
      provides: "Async SQLAlchemy engine, session factory, pgvector type registration, SQLAlchemy OTel instrumentation"
      exports: ["get_engine", "AsyncSessionLocal", "get_session"]
    - path: "app/models.py"
      provides: "Document SQLAlchemy model with vector(384) embedding column"
      contains: "class Document"
    - path: "alembic/versions/0001_create_documents.py"
      provides: "Migration creating the vector extension and documents table"
      contains: "CREATE EXTENSION IF NOT EXISTS vector"
  key_links:
    - from: "app/db.py"
      to: ".env.example / DATABASE_URL"
      via: "os.getenv('DATABASE_URL', DEFAULT_DATABASE_URL) building the async engine"
      pattern: "DATABASE_URL"
    - from: "app/models.py"
      to: "app/embeddings.py"
      via: "Document.embedding declared as Vector(EMBEDDING_DIM) imported from app.embeddings"
      pattern: "Vector\\(EMBEDDING_DIM\\)"
    - from: "app/db.py"
      to: "pgvector adapter"
      via: "event.listens_for(engine.sync_engine, 'connect') registering the vector type (asyncpg needs explicit registration)"
      pattern: "listens_for"
---

<objective>
Stand up the RAG datastore foundation on top of Phase 1's FastAPI/OTel scaffold: a pgvector-enabled Postgres container, an async SQLAlchemy engine wired for free DB spans, the `Document` model with a 384-dim vector column, a local sentence-transformers embedding module, and an Alembic migration applied to the live database.

Purpose: Every downstream plan (corpus seeding in 02-02, retrieval in 02-04) needs a live pgvector schema and a single source of truth for the embedding dimension. This plan produces that substrate and proves the migration is actually applied to the running database (not just present as a file).

Output: A running `rag-postgres` container, `app/embeddings.py`, `app/db.py`, `app/models.py`, an Alembic setup with one applied migration, and updated `requirements.txt` / `.env.example` / `docker-compose.yaml`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.claude/CLAUDE.md
@.planning/phases/02-rag-service-core/02-CONTEXT.md
@.planning/phases/02-rag-service-core/02-PATTERNS.md
@app/telemetry.py
@requirements.txt

<interfaces>
<!-- Established Phase 1 patterns the executor must mirror (from app/telemetry.py). -->
<!-- Provider/infra setup module shape: a DEFAULT_* module constant + a setup/get function that reads an env var with load_dotenv(), called once from app/main.py. -->
<!-- Tracer acquisition: obtain tracers via opentelemetry.trace.get_tracer(__name__); NEVER construct a new TracerProvider here — telemetry.py already registered the global provider. -->

app/telemetry.py exposes:
- setup_telemetry() -> None   (already registers global Tracer/Meter/Logger providers; do NOT duplicate)
- module constants DEFAULT_OTLP_ENDPOINT, DEFAULT_SERVICE_NAME (pattern to mirror for DEFAULT_DATABASE_URL)

CLAUDE.md locked version-compatibility notes (authoritative for this plan):
- postgresql+asyncpg:// connection URL; async engine via create_async_engine
- register the pgvector type via event.listens_for(engine.sync_engine, "connect") calling register_vector (asyncpg needs explicit registration, unlike psycopg2)
- opentelemetry-instrumentation-sqlalchemy==0.65b0 pins to the same 0.65b line as the other contrib packages already installed
- embedding model sentence-transformers/all-MiniLM-L6-v2 = 384 dimensions (BAAI/bge-small-en-v1.5 is also 384 — so vector(384) is safe for either discretion option)
</interfaces>
</context>

<artifacts_produced>
## Artifacts this phase produces (Plan 02-01)

New files:
- `docker-compose.yaml` — RAG datastore compose (service `rag-postgres`, image `pgvector/pgvector:pg16`, host port 5432)
- `app/embeddings.py`
- `app/db.py`
- `app/models.py`
- `alembic.ini`, `alembic/env.py`, `alembic/script.py.mako`, `alembic/versions/0001_create_documents.py`

New symbols (available to downstream plans 02-02 and 02-04):
- `app/embeddings.py`: `EMBEDDING_DIM` (int = 384), `DEFAULT_EMBEDDING_MODEL` (str), `get_model()`, `embed_text(text: str) -> list[float]`, `embed_texts(texts: list[str]) -> list[list[float]]`
- `app/db.py`: `DEFAULT_DATABASE_URL` (str), `get_engine()`, `AsyncSessionLocal` (async_sessionmaker), `get_session()` (FastAPI async dependency yielding an AsyncSession), `setup_db_instrumentation()`
- `app/models.py`: `Base` (DeclarativeBase), `Document` (columns: `id` int PK, `doc_id` str unique, `title` str, `body` text, `embedding` Vector(EMBEDDING_DIM)); table name `documents`
- New env vars: `DATABASE_URL`, `LLM_PROVIDER`, `GROQ_API_KEY`, `CEREBRAS_API_KEY`, `GEMINI_API_KEY`, `EMBEDDING_MODEL` (LLM vars are declared here so `.env.example` has a single owner; they are consumed in plans 02-03/02-04)
- New packages pinned in `requirements.txt`: `sqlalchemy==2.0.51`, `asyncpg==0.31.0`, `pgvector==0.5.0`, `alembic==1.18.5`, `sentence-transformers==5.6.0`, `openai==2.46.0`, `opentelemetry-instrumentation-sqlalchemy==0.65b0`, `httpx`, `pytest`, `pytest-asyncio`
</artifacts_produced>

<tasks>

<task type="auto">
  <name>Task 1: Add pinned dependencies, the pgvector Postgres container, env config, and the local embedding module</name>
  <files>requirements.txt, docker-compose.yaml, .env.example, app/embeddings.py</files>
  <read_first>
    - requirements.txt (current pinned Phase 1 deps — append, do not rewrite)
    - .claude/CLAUDE.md sections "Recommended Stack" and "Version Compatibility" (authoritative pinned versions; these ARE the package legitimacy audit of record — every version below was verified via the PyPI JSON API with HIGH confidence, dated 2026-07-20)
    - app/telemetry.py (mirror its DEFAULT_* constant + load_dotenv() + os.getenv pattern in app/embeddings.py)
  </read_first>
  <action>
    Append these exact pinned packages to requirements.txt (keeping the existing Phase 1 lines intact): sqlalchemy==2.0.51, asyncpg==0.31.0, pgvector==0.5.0, alembic==1.18.5, sentence-transformers==5.6.0, openai==2.46.0, opentelemetry-instrumentation-sqlalchemy==0.65b0, httpx, pytest, pytest-asyncio. Installing at these exact pins IS the supply-chain legitimacy check (T-02-SC): a hallucinated/typosquatted name or nonexistent version fails pip install immediately — do not relax the == pins.

    Create docker-compose.yaml at repo root defining ONE service named rag-postgres using image pgvector/pgvector:pg16, mapping host port 5432 to container 5432 (confirmed free — SigNoz's metastore Postgres is internal-only and not host-published), with environment POSTGRES_USER=agentk, POSTGRES_PASSWORD=agentk, POSTGRES_DB=agentk, a named volume for /var/lib/postgresql/data, and a healthcheck invoking pg_isready -U agentk. This is a separate committed compose file for the RAG datastore ONLY — do not touch the Foundry-generated pours/ compose (it is gitignored and regenerable).

    Append to .env.example (single owner of env docs for this phase): DATABASE_URL set to postgresql+asyncpg://agentk:agentk@localhost:5432/agentk, LLM_PROVIDER set to groq (per D-08 default), empty GROQ_API_KEY, empty CEREBRAS_API_KEY, empty GEMINI_API_KEY, and EMBEDDING_MODEL set to sentence-transformers/all-MiniLM-L6-v2. Keep the existing OTEL_* keys. The *_API_KEY values stay empty in the committed example — real secrets live only in the gitignored .env.

    Create app/embeddings.py following telemetry.py's module shape: constant DEFAULT_EMBEDDING_MODEL = "sentence-transformers/all-MiniLM-L6-v2" (per D-03 whole-doc embedding, discretion default; swappable to BAAI/bge-small-en-v1.5, same 384 dims), constant EMBEDDING_DIM = 384, get_model() returning a lazily-instantiated module-singleton SentenceTransformer built from os.getenv("EMBEDDING_MODEL", DEFAULT_EMBEDDING_MODEL) after load_dotenv(), embed_text(text) returning get_model().encode(text, normalize_embeddings=True).tolist(), and embed_texts(texts) for batch encoding. Normalize embeddings so cosine distance is meaningful for the pgvector search in 02-04. This module is the single source of truth for the embedding dimension that models.py and the migration MUST match, and it runs entirely locally (satisfies RAG-02's "no external embedding API" — sentence-transformers loads weights from the local HF cache and makes no inference API call).
  </action>
  <verify>
    <automated>python -m venv /tmp/agentk-p2 >/dev/null 2>&1; . /tmp/agentk-p2/bin/activate 2>/dev/null; pip install -q -r requirements.txt && python -c "from app.embeddings import embed_text, EMBEDDING_DIM; v=embed_text('how do I reset my API key'); assert EMBEDDING_DIM==384 and len(v)==384, len(v); print('embeddings OK', len(v))"</automated>
    <automated>docker compose up -d rag-postgres && sleep 5 && docker compose exec -T rag-postgres pg_isready -U agentk</automated>
  </verify>
  <acceptance_criteria>
    - pip install -r requirements.txt completes with no unresolved package or version (proves every pinned package + version exists on PyPI — supply-chain gate T-02-SC).
    - python import of app.embeddings.embed_text returns a 384-length vector and EMBEDDING_DIM equals 384, with no network call to any embedding API.
    - docker compose exec -T rag-postgres pg_isready -U agentk reports the server is accepting connections.
    - grep confirms docker-compose.yaml contains the literal pgvector/pgvector and .env.example contains DATABASE_URL, LLM_PROVIDER, GROQ_API_KEY, CEREBRAS_API_KEY, and GEMINI_API_KEY.
    - requirements.txt still contains the original Phase 1 pins (fastapi==0.139.2 and opentelemetry-api==1.44.0) plus the new pins.
  </acceptance_criteria>
  <done>Dependencies install cleanly at their locked pins, the pgvector Postgres container is healthy, .env.example documents all new vars, and app/embeddings.py produces 384-dim local vectors.</done>
</task>

<task type="auto">
  <name>Task 2: Create the async SQLAlchemy engine, the Document model, and the Alembic migration file</name>
  <files>app/db.py, app/models.py, alembic.ini, alembic/env.py, alembic/script.py.mako, alembic/versions/0001_create_documents.py</files>
  <read_first>
    - app/embeddings.py (import EMBEDDING_DIM — the vector column width MUST equal this)
    - app/telemetry.py (mirror the setup-function + DEFAULT_* + load_dotenv pattern; do not duplicate provider setup here)
    - .claude/CLAUDE.md section "Version Compatibility" (async engine + pgvector register_vector via event.listens_for(engine.sync_engine, "connect"); opentelemetry-instrumentation-sqlalchemy usage)
    - .planning/phases/02-rag-service-core/02-PATTERNS.md section "app/db.py" (structural shape borrowed from telemetry.py)
  </read_first>
  <action>
    Create app/models.py: Base as a DeclarativeBase subclass, and Document mapped to table documents with columns id (Integer primary key), doc_id (String, unique, not null — a stable slug used in the D-04 sources contract), title (String, not null), body (Text, not null), and embedding typed as pgvector's Vector(EMBEDDING_DIM) importing EMBEDDING_DIM from app.embeddings (never hardcode the dimension in two places — single source of truth).

    Create app/db.py mirroring telemetry.py's shape: constant DEFAULT_DATABASE_URL = "postgresql+asyncpg://agentk:agentk@localhost:5432/agentk"; get_engine() building a module-singleton via create_async_engine(os.getenv("DATABASE_URL", DEFAULT_DATABASE_URL)) after load_dotenv(); register the pgvector type on connect using event.listens_for(engine.sync_engine, "connect") calling pgvector's register_vector (asyncpg requires explicit registration per CLAUDE.md); AsyncSessionLocal built via async_sessionmaker(engine, expire_on_commit=False); an async FastAPI dependency get_session() that yields an AsyncSession inside an async context manager; and setup_db_instrumentation() calling SQLAlchemyInstrumentor().instrument(engine=engine.sync_engine) so the pgvector similarity query in 02-04 emits a free DB span underneath the hand-written rag.retrieval span (D-07 — layer on top of, do not replace, the auto DB spans).

    Initialize Alembic for async: create alembic.ini (script_location = alembic; read sqlalchemy.url from the DATABASE_URL env var in env.py rather than hardcoding the password in alembic.ini), alembic/script.py.mako (standard template), and alembic/env.py using the async engine template (run_migrations_online creating an async engine from DATABASE_URL, with target_metadata set to Base.metadata imported from app.models). Author migration alembic/versions/0001_create_documents.py whose upgrade() runs op.execute with the literal CREATE EXTENSION IF NOT EXISTS vector FIRST, then creates the documents table including an embedding column of type vector(384) (via pgvector's SQLAlchemy Vector type or raw DDL), and whose downgrade() drops the documents table. Do NOT run the migration in this task — applying it to the live DB is Task 3's BLOCKING responsibility.
  </action>
  <verify>
    <automated>. /tmp/agentk-p2/bin/activate 2>/dev/null; python -c "from app.models import Document, Base; from app.embeddings import EMBEDDING_DIM; c=Document.__table__.c; assert set(['id','doc_id','title','body','embedding']).issubset(set(c.keys())), list(c.keys()); print('model cols OK', list(c.keys()))"</automated>
    <automated>grep -q "CREATE EXTENSION IF NOT EXISTS vector" alembic/versions/0001_create_documents.py && echo "migration creates extension OK"</automated>
  </verify>
  <acceptance_criteria>
    - Importing app.models.Document succeeds and its table has columns id, doc_id, title, body, embedding.
    - app/models.py declares the embedding column width from app.embeddings.EMBEDDING_DIM (no second hardcoded 384 in models.py).
    - app/db.py contains a call to event.listens_for on engine.sync_engine and a SQLAlchemyInstrumentor().instrument(engine=...) call.
    - alembic/versions/0001_create_documents.py contains the literal CREATE EXTENSION IF NOT EXISTS vector and creates the documents table before any index on the embedding column.
    - alembic/env.py imports Base.metadata from app.models and builds its engine from the DATABASE_URL env var (no hardcoded credentials in alembic.ini).
  </acceptance_criteria>
  <done>The Document model, async engine with pgvector registration + SQLAlchemy instrumentation, and a runnable Alembic migration file all exist and import cleanly — but the migration is not yet applied.</done>
</task>

<task type="auto">
  <name>Task 3 [BLOCKING]: Apply the migration to the live database and verify the schema exists</name>
  <files>alembic/versions/0001_create_documents.py</files>
  <read_first>
    - alembic.ini and alembic/env.py (confirm sqlalchemy.url resolves from DATABASE_URL against the running rag-postgres container)
    - docker-compose.yaml (confirm the rag-postgres service name and credentials)
  </read_first>
  <action>
    This BLOCKING task exists because build/import checks pass without the migration actually being applied to the live database (SQLAlchemy models and Alembic files are static Python — nothing proves the documents table or the vector extension exist in the running Postgres until the migration runs against it). Ensure the rag-postgres container from Task 1 is running, then run alembic upgrade head against it (with DATABASE_URL pointing at localhost:5432 as in .env.example). Confirm the migration reached head and that the live database now has both the vector extension and the documents table with a vector-typed embedding column. This task must complete BEFORE any downstream verification (02-02 seeding, 02-04 retrieval) — those steps query the live DB and will silently mislead if the schema was never applied.
  </action>
  <verify>
    <automated>. /tmp/agentk-p2/bin/activate 2>/dev/null; alembic upgrade head && alembic current | grep -q "0001" && echo "alembic at head OK"</automated>
    <automated>docker compose exec -T rag-postgres psql -U agentk -d agentk -c "\dx vector" | grep -q vector && docker compose exec -T rag-postgres psql -U agentk -d agentk -c "\d documents" | grep -qi "vector(384)" && echo "live schema OK: extension + documents.embedding vector(384)"</automated>
  </verify>
  <acceptance_criteria>
    - alembic upgrade head exits 0 and alembic current reports revision 0001 at head.
    - psql \dx lists the vector extension as installed in the agentk database.
    - psql \d documents shows the embedding column typed as vector(384) in the live database.
    - Re-running alembic upgrade head is a no-op (idempotent — already at head), confirming the migration is not double-applied.
  </acceptance_criteria>
  <done>The vector extension and documents table (with a vector(384) embedding column) are confirmed present in the running rag-postgres database, unblocking corpus seeding in 02-02.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build host -> PyPI | Untrusted package names/versions installed via pip |
| RAG app -> Postgres | Application connects with credentials over the DB connection string |
| host filesystem -> .env | Secrets (DB creds, later LLM keys) live in a gitignored env file |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-SC | Tampering | pip installs (sqlalchemy, asyncpg, pgvector, alembic, sentence-transformers, openai, opentelemetry-instrumentation-sqlalchemy, httpx) | high | mitigate | All packages are canonical PyPI names pre-verified in CLAUDE.md's stack table via the PyPI JSON API (HIGH confidence, dated 2026-07-20) — the audit of record. Install ONLY at the exact == pins; a nonexistent/typosquatted name or version fails pip install and blocks the task. No [ASSUMED]/[SUS]/[SLOP] packages present, so no per-package human checkpoint is required. |
| T-02-01 | Information Disclosure | .env / DATABASE_URL credentials | high | mitigate | .env stays gitignored (confirmed in .gitignore); .env.example ships empty API-key values and only the local dev DB credentials. Credentials are never logged or set as span attributes. |
| T-02-02 | Tampering | Alembic migration on live DB | medium | mitigate | Migration is reviewed, version-controlled (0001), idempotent (CREATE EXTENSION IF NOT EXISTS), and applied explicitly via the BLOCKING Task 3 rather than auto-run on app import. |
| T-02-03 | Denial of Service | pgvector Postgres container | low | accept | Local dev/demo container; single-tenant hackathon scope. No public exposure planned; host port 5432 bound for local access only. |
</threat_model>

<verification>
- `pip install -r requirements.txt` resolves all pins (T-02-SC supply-chain gate passes).
- `app.embeddings.embed_text` returns a 384-dim vector locally with no external API call (RAG-02 no-external-embedding constraint).
- `alembic upgrade head` applies cleanly and the live database reports the vector extension + a documents table with a vector(384) embedding column (schema-push BLOCKING gate).
- `app/db.py` registers the pgvector type on connect and instruments the engine for free DB spans (D-07 substrate).
</verification>

<success_criteria>
- A running rag-postgres (pgvector) container reachable at DATABASE_URL.
- The documents table + vector extension exist in the live DB (verified via psql, not just via file presence).
- app/embeddings.py, app/db.py, app/models.py import cleanly and share EMBEDDING_DIM=384 as the single dimension source of truth.
- All new dependencies pinned and installable; .env.example documents every new env var.
</success_criteria>

<output>
Create `.planning/phases/02-rag-service-core/02-01-SUMMARY.md` when done.
</output>
